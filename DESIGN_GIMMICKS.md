# ギミック拡張 設計書

作成: 2026-09-04 / 最終更新: 実装完了時に実測値へ改訂 / 対象: 本リポジトリ

> この文書は実装前の設計案に、実装で判明した事実を反映させた版です。
> `DESIGN.md` と同じ書式で、実装で覆った箇所には「実測」と明記しています。
> **設計案のまま残っている部分はありません。** 全部作って、全部測りました。

---

## 0. 診断 — なぜ拡張前のゲームは退屈だったのか

### 0.1 プレイヤーが決められることが何もなかった

入力は「投入する x 位置」と「タイミング」の2つだけで、どちらも結果に効きませんでした。

- 投入した1枚は上段の山に飲まれ、数十秒かけて山全体の圧力の一部になる。
  どこに落としたかは山が均されるまでの数秒で消える
- 抽選の引き金はボールの落下だが、`DESIGN.md §7.4` の実測表が示すとおり、
  ボールの落下頻度を決めていたのは **`ball.spawnZ`（＝台の定数）**。
  プレイヤーの操作は変数に入っていなかった

### 0.2 積み上がるものが何もなかった

台が持つ状態は「保留 0〜3」だけで、しかも数秒で消化される。
30秒前と今とで台の状態が変わらない ＝ 期待が育つ場所がない、ということです。

### 0.3 当たっても台の挙動が変わらなかった

`DESIGN.md §7.4` の実測: 5分でスロット13回・当たり10回・払い出し39枚。
同じ5分にプッシャーからは397枚。**スロットは経済の1割弱**でした。

見せ場は「山が崩れてメダルが滝のように落ちる」ことなのに、
当たり演出はその見せ場を一切引き起こさない。液晶で数字が動くだけでした。

### 0.4 入れたもの

| 欠けていたもの | 入れたもの |
|---|---|
| 狙える的 | **チャッカー**（§3.1）／**フリッパー**（§3.6）／**台パン**（§3.8） |
| 当たったときの手応え | **配当の引き上げ**（§4.3）／**演出の多層化**（§3.9） |
| 積み上がるメーター | **保留オッズ**（§3.2）／**プログレッシブJP**（§3.4） |
| 台の挙動が変わる山場 | **フィーバー**（§3.3）／**メダルタワー**（§3.5） |
| デジタルならではの層 | **特殊メダル**（§3.7） |
| 待つこと自体が楽しい役物 | **3段クルーン**（§3.11） |

---

## 1. 調べて分かったこと — 実機は「抽選のピラミッド」でできている

スピンフィーバー（コナミ）、スマッシュスタジアム（コナミ）、バベルのメダルタワー（セガ）は、
機種が違っても構造が同じでした。**単発の抽選が無く、必ず多層です。**

```
  第5層  ジャックポット        累積した数百〜数千枚。ごく稀。台を止めて演出する
     ↑
  第4層  ボーナスゲーム        30秒〜数分。台の挙動そのものが変わる
     ↑
  第3層  ステップ              「ボール3個落とす」など、数分かけて溜める
     ↑
  第2層  スロット抽選          十数秒に1回。小配当＋上位層への昇格抽選
     ↑                        （保留が溢れたら倍率に化ける）
  第1層  チャッカー通過        物理。プレイヤーが狙う。数秒に1回
```

**要点は「上の層に上がる権利」を下の層が配っていること。** どの層にも空振りが無い。

### 1.1 具体例（調査メモ）

- **スピンフィーバー**: **下段の払い出しスロープ**に丸穴の *スタートチェッカー* があり、
  ここにメダルが入るとスロットが回る。**保留は最大10**。満杯からさらに入ると
  **当選時の払い出しが 15枚→30枚→50枚に増える**。上部の *チャンスチェッカー* を通すと
  **スタートチェッカー脇のフリッパーが動いて入りやすくなる**。
  ボール1個落下で1ステップ、3ステップでフィーバー
- **スマッシュスタジアム**: 保留10到達後は通過ごとに**オッズが5段階まで上昇**。
  ボール5個落下でフィーバーラッシュ。JPは **投入1枚 = +0.02枚**、
  **バンパー1ヒット = +0.15枚** で積み上がるプログレッシブ
- **バベルのメダルタワー**: JPチャンスで**プッシャーが引っ込み、
  獲得枚数に応じてメダルのタワーが物理的に建設される**
- **クルーン**: 当たり穴に入るまでの**数秒〜十数秒の「待つ時間」そのものを商品にしている**役物

### 1.2 【最重要】スピンフィーバーがチェッカーを「盤面」に置いていない理由

調査時は読み流していた一行が、実装で決定的に効きました。

> プッシャーフィールド**下段のメダルの払い出し口が傾斜となっており**、
> スタートチェッカー（スロット回転のための丸い穴）があり

**チェッカーは盤面ではなく、払い出し口の坂の上にあります。**
なぜそうなっているのか、実装して初めて分かりました（§3.1）。

### 1.3 このプロジェクトが持っている、実機に無い武器

**全部が本物の剛体だということ。** タワーを建てるのに専用メカは要らず、
`MedalPool.spawn()` を呼ぶだけで本物の塔が建って本物に崩れます。
台を傾けるのもストロークを変えるのも `config.js` の数字を動かすだけ。

したがって設計方針は1つ。

> **ボーナスは液晶の中ではなく、盤面の物理で表現する。**

---

## 2. 全体像（実装後）

```
   メダル ──► 払い出しスロープの START チャッカー ──► スロット（保留・倍率）──► 小〜中配当
      │                          予告→擬似連→カットイン→滑り (§3.9)   └► 青7 = JPチャンス
      │      └► CHANCE チャッカー ──► フリッパーが開く / JPメーター +1
      │
      └──► サイドポケット（ロスト）──► JPメーター +1 / 50枚ごとにチケット1枚

   ボール ──► 払い出し口に落下 ─┬► STEP +1 ──► 3個で ★フィーバー(25秒)★
                                │                   │
                                │   台が手前に傾く／ストローク拡大／ポケット閉鎖
                                │                   └► 明けにボム1枚
                                │
                                └► リフトで役物へ ──► ★3段クルーン★ (§3.11)
                                     皿1枚につき穴5つ・当たり1つ。3枚抜けて固定300枚

   青7 ──► ★ジャックポット★ プッシャー後退 → タワー建設(180枚) → 一斉崩落 → 残りをホッパー
```

**メダルとボールの役割を分けたのが肝**です。
メダルは短周期（数秒）の抽選、ボールは長周期（数分）のステップに専念させています。

---

## 3. 各ギミック（実装後）

### 3.1 チャッカー — 狙える的【最重要の設計変更あり】

#### 何が起きるか

前縁（z=6.0）の先に**手前へ下る坂（払い出しスロープ）**を置き、その上に穴を3つ開けています。

| 穴 | 位置 | 奥行き | 入るとどうなるか |
|---|---|---|---|
| **START** | 中央 x ±0.55 | 1.10 | スロットが1回まわる |
| **CHANCE** ×2 | x ±2.45〜3.55 | 0.70 | フリッパーが8秒開く + JPメーター +1 |

入ったメダルは**飲まれます**（クレジットにならない）。
実機のチャッカーも機内のホッパーに落ちるので払い出されません。
「1枚差し出して抽選を1回買う」という取引にしておかないと、
中央を狙うことが一方的に得になってゲームになりません。

#### 【実測・設計変更】穴を盤面に開けたら機械が止まった

**当初の設計は「手前テーブルの前縁に切り欠きを作る」でした。これは完全な失敗です。**

前縁なら「もう落ちる直前の、単独で滑ってくる1枚だけが入る」つもりでした。
実際には、**穴は山の中にあります。**

| | 場内の平衡枚数 | 60秒あたりの払い出し | AntiJam |
|---|---|---|---|
| 穴なし | 231 | 63枚 | 0 |
| **前縁に穴（当初案）** | **350〜410** | **12枚** | **4回** |
| 坂に穴（採用） | 205 | 63枚 | 0 |

理由は `DESIGN.md §2.4` が上段について書いているのと同じです。

> 隙間があるとそこで力の連鎖が切れ、前方の山は動かない

**山は力の連鎖で前へ進むので、途中でメダルが消えると連鎖が切れます。**
前縁まで力が伝わらなくなり、払い出しそのものが止まりました。
場内が膨らみ続け、最後はアーチを組んで完全に固着します。

**払い出しスロープなら、山の力学に一切触れません。**
坂の上は「もう払い出されたメダルが1枚ずつ滑り落ちてくる」場所だからです。
実機がチェッカーを払い出し口の傾斜に置いているのは、おそらくこれが理由です（§1.2）。

#### 【実測】入賞率を決めるのは幅ではなく「奥行き」

設計時は「幅に比例する」と見積もっていました（当初の §8-1）。外れました。
効くのは z 方向の長さで、**メダル直径 1.0 をまたぐと桁が変わります**。

| 奥行き | 幅 | START の入賞 |
|---|---|---|
| 1.15 | 1.24 | **34回/分** ほぼ全部落ちる |
| 0.92 | 1.24 | **9回/分** ときどき橋を架けて跨ぐ |
| 0.85 | 1.10 | **4回/分** ふつうは跨ぐ（CHANCE に採用）|

0.23 縮めただけで 1/4 になります。
幅は「メダルが挟まらない」ための下限（直径 + 0.2）として効くだけで、頻度の調整には使えません。

**チャッカーの頻度は演出の処理能力（1回 約2.5秒 = 24回/分）より下に置くこと。**
超えると保留が常に満杯になり、倍率が最大に張り付いて経済が壊れます（§3.2）。

#### 坂の形

```
     テーブル前縁 z=6.0, y=+0.18
          │
          ↓  ┌──────────────┐  ← 坂 28.7°（メダルの静止摩擦 0.26 = 14.6° 相当なので確実に滑る）
             │   ▓▓  ●  ▓▓  │     天面 (6.05, -0.30) → (8.24, -1.50)
             └──────────────┘
                            ↓ 先端から受け皿へ = クレジット
```

- 坂は Fixed 剛体1つ。穴も縁もフリッパーも全部この剛体にぶら下げている
- 幅 11.5（テーブルの 11.0 より少し広い）。左右に縁を立てて横へこぼさない
- **判定は座標だけ**（センサー不要）。穴を抜けたメダルは z < 7.9 で、
  先端から落ちたメダルは z > 8.3 で `fallY` を跨ぐので、`splitZ = 8.05` に十分な余裕がある

#### 触ったファイル

`config.js`（`chute`）／ 新規 `game/PayoutChute.js` ／ `game/TiltTable.js`（穴を持たない一枚板に戻した）／
`game/Payout.js`（3分類）／ `game/Cabinet.js`（坂に掛かる装飾を移動）／ `main.js`

---

### 3.2 保留オッズ — 溢れた抽選を捨てない

保留（最大4）が満杯のときの入賞を、捨てずに倍率へ回します。

```
保留に空きがある → 保留 +1
保留が満杯       → 溢れカウンタ +1。oddsStep(4) 回ごとに倍率が1段上がる（×1→×2→×3→×5）
当選した         → 倍率は使い切り。×1 に戻る
```

#### 【実測】溢れをそのまま倍率にすると張り付く

当初は「溢れたら即1段」でした。チャッカーが演出より速い場面で破綻します。

| | 平均倍率 | 総払い戻し |
|---|---|---|
| 溢れ1回 = 1段（当初案）| **×2.78** | **151%** |
| 溢れ4回 = 1段（採用）| ×1.0〜1.2 | 68% |

チャッカー 34回/分 に対して演出は 24回/分 しか捌けないので、
差分の10回/分がそのまま倍率に化けて最大に張り付いていました。
**倍率は「調子が良いときのご褒美」であって、常態であってはいけません。**

---

### 3.3 フィーバー — 台の挙動そのものが変わる【本命】

**ボールが払い出し口に落ちると STEP +1。3つでフィーバー突入（25秒）。**
（サイドポケットに落ちたぶんはステップにならない）

フィーバー中に起きること、**すべて物理**です。

| 効果 | 変更 | 根拠 |
|---|---|---|
| ① 落とし口の勾配が反転 | `tableFront.rx`: `-0.06` → `+0.04`（1.2秒かけて）| §7.6 は勾配で払い戻しが上がったと実測。逆に倒せば山ごと手前に流れる |
| ② ストローク拡大 | `strokeHalf` 0.95→1.25 / `period` 1.9→1.5 | §5 の実測表で「出過ぎる」と判定して通常時に採用しなかった設定を、ここでだけ解禁 |
| ③ サイドポケットが塞がる | シャッターを2枚せり上げる | 実測のロスト率がゼロになる |
| ④ 場が痩せたら補給 | 場内 150 を下回ったら 6枚/秒 | ①②で出過ぎたぶんを埋める |

#### 実測（25秒 × 1回）

| | 通常時 | フィーバー中 |
|---|---|---|
| 払い出し | 26枚 / 25秒 | **71枚 / 25秒**（2.7倍）|
| ロスト | 8枚 | **0枚** |
| 場内 | 205 | 205 → 165 |
| 傾き / ストローク | -0.06 / 0.95 | +0.04 / 1.25 |

明けは全部きれいに戻ります（傾き -0.06、ストローク 0.95、シャッター格納）。

#### 【実測】プッシャーの位相は積分で持つこと

旧実装は `z = baseZ + strokeHalf * sin(2π t / period)` と**絶対時刻から位相を計算**していました。
この形のまま `period` を 1.9 → 1.5 に変えると**位相が飛び、プッシャーが瞬間移動します**
（`DESIGN.md §7.1` が `setTranslation()` を禁じているのと同じ事故）。

```js
this.phase += (2 * Math.PI * dt) / this.period;   // period が変わっても位相は連続
z = this.baseZ + this.strokeHalf * Math.sin(this.phase);
```

振幅のほうは位相が連続なら途中で変えても飛びませんが、
段差が出るので 0.25 秒の時定数で滑らかに寄せています。
`syncMesh()` が「解析的に求まるので誤差ゼロ」だった利点は失われるので、
描画側は物理の `prevZ / z` を `alpha` で補間する形に変えました。

#### 傾きを変えるときは y も一緒に動かす

中心で回すと後端が沈み、`tableMain`（z=3.0）との間に段差ができてメダルがこぼれます。

```
y(rx) = -0.5 + 1.5 * sin(-rx)      rx=-0.06 → -0.410 / rx=+0.04 → -0.560
```

**1.2秒より速く回さないこと。** 一気に回すと山に運動エネルギーを注入して吹き飛びます。

---

### 3.4 プログレッシブ・ジャックポット

液晶の左上に常時表示。遊んでいる間ずっと増え、当たると全部出て初期値に戻ります。

| 加算源 | 加算量 | 実測の寄与 |
|---|---|---|
| メダル投入 | 1枚 = +0.05 | 約 5.5/分 |
| **サイドポケットにロスト** | 1枚 = **+1.0** | 約 12/分 |
| チャンスチャッカー通過 | 1回 = +1.0 | 約 5/分 |
| | | **合計 約 20〜38/分** |

初期値 200／上限 800。当選は**青7**（weight 3/1000、実測 約13分に1回）。
当選時の平均額はおよそ 450〜550枚になります。

**サイドポケットのロストを主要な燃料にしたのが要点です。**
実測でロストは払い出しの 9〜17% あるのに、いままではただ減るだけでした。
ここを JP の燃料にすると、いちばん腹が立つ現象がいちばん期待を煽る現象に変わります。

JP の現在額は**セーブに含めます**。開くたびに初期値へ戻ると積み立ての意味が消えるためです。

---

### 3.5 メダルタワー — 山場の見た目

JP 当選時、バベルのメダルタワーと同じ手順を踏みます。

```
  park  (2秒)  プッシャーが最後方まで下がって停止。台が静まる
  build (9秒)  後退したプッシャーの上面に 3×3 の束で 180枚を積む（24枚/秒）
  sweep (3秒)  通常より遅く、大きく前進して崩す
  pay   (18秒) 積みきれなかったぶんを通常のホッパーで払う
```

- **1本柱にしない。** メダル厚 0.10 で 180枚を1本に積むと 18 unit で天井（y=16）を突き抜ける。
  3×3 の束にして 20層 = 2.0 unit の塔にしている
- 積む位置は**後退したプッシャーの上面（z = -1.2）**。ここを外すと sweep で運ばれず置き去りになる
- 既に山があるので、その上（y0 = 2.6）から落とす。山に食い込む高さで生成すると剛体が弾け飛ぶ
- **建設中だけ `solverIterations` を 8 → 14 に上げる。** 薄い円盤を20層積むのは 8 では厳しい

#### 【実測】ホッパーの「詰まり時に上限を無視する」が事故を起こす

`Hopper` には「`fieldLimit`(330) で3秒出せなかったら上限を無視する」という逃げ道がありました。
小さな払い出しでは問題になりませんが、**JP の残り 340枚でこれが暴発します。**

| | 場内のピーク | 1ステップ相当 |
|---|---|---|
| 上限を無視（旧）| **581枚**（プール上限 585）| **約 28ms** |
| 超過を 60枚までに制限（採用）| 390〜403枚 | 約 15ms |

**逃げ道は要りますが、逃げ道にも上限が要ります。** `stallOverflow: 60` を足しました。
`jackpot.max` も 1200 → 800 に下げています（出し切るのに時間が掛かりすぎるため）。

`towerLimit: 180` は `DESIGN.md §7.6` の実測（400枚→16.2ms）から逆算した値です。
**ここを超えて積んではいけません。**

---

### 3.6 フリッパー — 狙いを「操作」に変える

CHANCE チャッカーを通すと8秒間開き、START チャッカーへ向かう漏斗を作ります。

```
  閉 (通路 3.50)          開 (入口 2.42 → 出口 1.08)
     ┃    ●    ┃              ╲   ●   ╱
```

「まず左右を狙って漏斗を開き、開いている間に中央へ流し込む」という2手先の操作が生まれます。

#### 【実測】盤面の上に常設した板は、向きに関係なく機械を止める

当初は手前テーブルの上に、流れと平行に寝かせて常設していました。
「平行なら邪魔しない」という直感は完全に外れます。

| | 払い戻し | 場内 |
|---|---|---|
| 盤面に常設（当初案）| **15%** | **368枚** |
| 盤面から撤去 | 75% | 352枚 |
| 払い出しスロープに設置（採用）| 68% | 205枚 |

`DESIGN.md §7.7` の「手前の壁は機械を止める」（払い戻し 36%）とまったく同じ現象です。
チャッカーごと坂へ移したことで消えました。**坂の上なら常設しても機械は止まりません。**

#### 板は回すだけ。上下させない

坂の上には山が無いので、スリットも収納機構も要りません。
坂のローカル Y 軸（＝坂の法線）まわりに回すだけです。
姿勢は `坂の傾き × 開き角` のクォータニオン合成で作っています。

**閉じているときの間隔を広く保つこと。** 実測で x=1.55 だと閉じているだけで
START が 34回/分、x=1.90 で 21.6回/分。閉じた状態でも漏斗として効いてしまいます。
そこで **板を長く（1.70）して角度を大きく（0.90 rad）**し、
閉のときは 3.50 幅、開のときだけ出口 1.08 まで絞る形にしました。

---

### 3.7 特殊メダル — デジタルでしかできない層

剛体もマテリアルも増やさず、`InstancedMesh` の頂点色を差し替えるだけで作れます。

| 種類 | 入手 | 効果 |
|---|---|---|
| **ゴールド**（金）| スロットのリプレイ役で1枚 | 払い出し口に落ちると **+5枚** |
| **ボム**（赤）| フィーバー終了時に1枚 | 着弾 0.7 秒後に半径3以内へインパルス。**詰まりを自分で崩せる** |
| **チケット**（白）| ロスト50枚ごとに1枚 | 落ちると**チャッカーを通さずスロットが1回まわる** |

- 手持ちは種類ごとに最大3枚。`1` `2` `3` キー（または ITEM パネルのクリック）で選ぶ
- **どこで使うかがプレイヤーの判断**になる。とくにボムは `AntiJam`（自動）と
  同じことをプレイヤーの意思で撃つものなので、実質は既存ロジックの再利用
- ボムは爆発後、**消さずに通常メダルへ戻す**。消すと持ち枚数が合わなくなる

実装上の注意: `instanceColor` は `active` 配列の並び順に書くので、回収で並びが変わる以上、
**特殊メダルが場に居る間は毎フレーム書き直す必要があります。**
1枚も居なければ書き込みごと省略しています（`specialCount === 0` で分岐）。

---

### 3.8 台パン — いちばん安くていちばん「らしい」

`B` キーで筐体を叩く。場内の全メダルに弱い上向き＋前向きのインパルスを与え、カメラも少し揺れます。

- クールダウン **20秒**。効かない間に4回以上叩くと **TILT（10秒間まったく投入できない）**
- 実質 `AntiJam` を手動で撃てるようにしただけで、新規ロジックはほとんど無い
- カメラの揺れは `OrbitControls.update()` の**後**に入れること。前に入れると打ち消される

§0.1 の「決められることが何もない」に対する、いちばん安い回答です。

---

### 3.9 スロットの演出 — 色で期待度を示す

#### 調べて分かったこと

実機の演出は、機種が違っても **「色で期待度を示す」という一本の約束**でできていました。

> 基本ルール：「白＜青＜黄＜緑＜赤＜金＜レインボー」
> レインボーは大当り濃厚の最強カラー

そのうえに、予告音・カットイン・擬似連・フリーズといった部品が乗ります。

| 部品 | 実機での役割 |
|---|---|
| **予告音** | レバーを叩いた瞬間に鳴る。通常/強の2段。ヤマサの機種では強予告音＋赤ランプで期待度 88.2% |
| **カットイン** | 画面を横切るエフェクト。「サイズや背景色で信頼度が変わる」 |
| **擬似連** | 止まりかけたリールが再始動する。連数が増えるほど期待度が上がる |
| **フリーズ** | レバーオンやリール回転中に突然止まる。**大当りが確定する最高峰のプレミアム演出** |
| **ニアミス（滑り）** | 当たり絵柄を1つ外して止める。研究では「近似ハズレは当たりと同じ報酬系を刺激する」とされる |

#### いちばん大事なのは「信頼度が本当に一致していること」

色を賑やかしで振ると、プレイヤーはすぐ「意味が無い」と学習して見なくなります。
そこで **結果を先に決めてから、その結果の格に応じた重みで heat（期待度）を引く**構造にしました。
順番が逆だと、演出と結果が独立になって信頼度が成立しません。

`CFG.slot.show.weights` から逆算した実際の信頼度:

| heat | 色 | 出現率 | **信頼度** | 出る演出 |
|---|---|---|---|---|
| 0 | 白 | 40.6% | **8.6%** | 無演出 |
| 1 | 青 | 26.1% | **36.2%** | 予告音 + 青枠 |
| 2 | 緑 | 17.6% | **59.9%** | 緑枠 + 擬似連1回 |
| 3 | 赤 | 10.7% | **76.0%** | 赤枠 + カットイン |
| 4 | 金 | 4.5% | **85.7%** | 金枠 + 擬似連2回 + カットイン |
| 5 | 虹 | 0.6% | **100%** | 虹枠 + フリーズ + カットイン |

重みは役の格ごとに持たせてあるので、**金や虹は「当たる」だけでなく「大きい」ことも示唆します**
（バー以上だと heat 4 以上が 50%、青7なら 90%）。
逆に heat 0 でも 8.6% は当たるので、「無演出だから外れ」ではありません。実機もそうなっています。

#### 1回の流れ

```
① 予告 (620ms)      heat>=1。色枠を出して予告音。heat 0 は無演出のまま回る
② 回転 (520ms)
③ 擬似連 (700ms×n)  heat>=2 で1回、heat>=4 で2回。止まりかけて再始動する
④ 第1停止 → 第2停止 (420ms)
⑤ リーチなら
     カットイン (675ms)  heat>=3。斜めの帯が画面を横切る
     フリーズ  (1100ms)  heat=5。全部止まって白く飛ぶ = 当たり濃厚
     引っ張り            弱 900 / 中 1500 / 強 1900ms。バー以上は必ず強
⑥ 第3停止
     ハズレのリーチは「滑り」(260ms)。当たり絵柄を一瞬見せてから1つずらす
⑦ 結果 (650〜2600ms)
```

平均 **3.6秒/回**（実測）。演出が長くなったぶん処理能力は 24回/分 → 17回/分 に落ちるので、
保留は 4 → 3、倍率の段上げも 4回 → 3回ぶんに詰めています。

#### 実装

`SlotDisplay` の Canvas 2D に描き足しただけで、新しいメッシュもマテリアルも増やしていません。

- 色枠 … 画面の縁を `strokeRect` で光らせる。虹だけ内側にもう1本引いて別格に見せる
- カットイン … `transform` で斜めに倒した帯 + 白抜き文字
- フリーズ … 画面全体を白で塗って `FREEZE` を抜く
- 滑り … 第3リールだけクリップした中にリール帯を4コマ描き、1コマぶん縦に送る（§3.10）
- ベゼル（3Dの枠）の `emissive` も heat の色に光らせて、液晶の外まで色を出す

音も heat で変わります（`Sound.notice(heat)` は heat が上がるほど高く・長く・倍音が増える）。

---

### 3.10 3x3 の盤面 — 5ライン化と SVG の絵柄

**盤面を 1列3リールから 3列 x 3段に広げ、5本のペイラインを見るようにしました。**

#### なぜ 5本なのか

3x3 では 横3 + 縦3 + 斜め2 の **8本**まで取れます。一般的な3x3スロットは
5本か8本のどちらかで、縦を入れるかどうかが分かれ目です。ここでは**縦を入れていません**。

> 1列が丸ごと1本のラインになると、リールを1つ止めた瞬間に当たりが確定します。
> 「3つ目を待つ」というスロットの骨格が、1本目の停止で壊れてしまう。

残った5本は 上段 / 中段 / 下段 / 右下がり / 右上がり。
`config.js` の `slot.lines` に「列ごとに、どの段を見るか」で書いてあります。

```js
lines: [ [0,0,0], [1,1,1], [2,2,2], [0,1,2], [2,1,0] ]
```

#### 出目は「結果を決めてから作る」

盤面を先に引いて配当を後から数える作りにすると、期待値が `weights` から
直接読めなくなり、§4 で実測して追い込んだ払い戻しの根拠が消えます。順番は今までと同じです。

```
① SlotMachine が絵柄とライン数を引く   ← ここで払い出し枚数が確定する
② SlotLines.buildGrid が「その結果になる盤面」を組み立てる
```

②は必ず①の通りの盤面を返します。狙っていないラインが偶然揃った場合は作り直すので、
**画面に出ている当たりと払い出しは常に一致します**（40,000回の抽出検査で不一致 0 / 幽霊当たり 0）。

#### 「ちょうど n 本」にならない組み合わせがある

ラインを選んでそのマスを埋めると、選んでいないラインまで巻き添えで成立することがあります。

| 選んだもの | 実際に成立する本数 | 理由 |
|---|---|---|
| 上段 + 下段 + 斜め1本 | **4本** | 四隅が埋まるので、もう1本の斜めも勝手に揃う |
| 上段 + 中段 + 下段 | **5本** | 9マス全部が同じ絵柄になる |

そのため `pickLineSet` は、選んだ組み合わせが本当に n 本になるかを
`closureCount` で数えてから返しています。4本を抽選の目に入れていないのは、
これが「狙って作れる形」ではなく副産物でしか出てこないためです。

#### 配当の振り直し

1回の抽選で複数ラインが同時に成立するので、pay を据え置くと払い戻しがライン数ぶん膨らみます。
**1ラインあたりの pay を約 2/3 に削って**釣り合わせました。

| 絵柄 | 重み(/1000) | 1ライン時代 | **実装値(1ラインあたり)** | 平均ライン数 | 寄与 |
|---|---|---|---|---|---|
| ブランク | 612 (旧640) | 0 | 0 | — | — |
| リプレイ | 172 (旧165) | 1 | 1 | 1.66 | 285 |
| チェリー | 108 (旧100) | 2 | 2 | 1.66 | 359 |
| ベル | 68 (旧62) | 6 | **4** | 1.48 | 403 |
| スイカ | 22 (旧18) | 18 | **12** | 1.48 | 391 |
| バー | 10 (旧8) | 40 | **26** | 1.15 | 299 |
| 赤7 | 5 (旧4) | 80 | **44** | 1.15 | 253 |
| 青7 | 3 | JP当選 | JP当選 | 1 | — |
| | | 期待値 1.70 | | | **期待値 1.99** |

**400,000回の実測: 期待値 1.989 枚/回 / 当選率 38.68% / JP 1/341。**
§4.2 の換算表でいう「期待値 2.00 → 払い戻し約92%」で、旧構成の 86% から
**当たる頻度（35.7% → 38.7%）と1回の額の両方をわずかに上げた**位置です。

ライン数の重み (`slot.lineWeights`) は役の格ごとに変えてあります。

| 格 | 1本 | 2本 | 3本 | 5本 | 平均 |
|---|---|---|---|---|---|
| small (リプレイ / チェリー) | 58 | 26 | 12 | 4 | 1.66 |
| mid (ベル / スイカ) | 66 | 24 | 8 | 2 | 1.48 |
| big (バー / 赤7) | 85 | 15 | 0 | 0 | 1.15 |

**格が上がるほど1本に寄せてあります。** 大きい役まで多ライン化すると、
1回の払い出しがホッパーの処理能力を超えるためです。
最大は 赤7 44枚 x 2ライン x 倍率5 = **440枚**で、1ライン時代の
80枚 x 倍率5 = 400枚 とほぼ同じに収めてあります（§5 の実測を参照）。

#### 滑りは本当に1コマ送っている

ハズレのリーチで見せる「滑り」は、揃っていた窓 `[T0,T1,T2]` から
リール帯を1コマ送った `[新しく降りてきた1枚, T0, T1]` を実際の出目にしています。
見た目だけずらしているのではなく、**1本のリール帯の上を本当に1コマ動かした形**です。

#### 絵柄を絵文字から SVG に置き換えた

| 理由 | |
|---|---|
| 環境差 | 絵文字は OS ごとに字形も色も違う。Windows と Mac で別のゲームに見える |
| 二重管理 | 液晶（Canvas）と配当表（DOM）で見た目を揃えられない |
| 解像度 | 3x3 で1マスが小さくなり、絵文字の細部が潰れる |

`render/SymbolArt.js` に 100x100 の座標系でパスを1組だけ置き、
**液晶は `Path2D` で、HUD は React の `<svg>` で、同じデータを読みます。**
片方だけ直して見た目がずれる、ということが起きません。

#### 画面の場所の取り合い

3x3 にすると液晶パネルの背が伸びます（2.6 → 3.62）。そのままだと
画面上部中央の PAY TABLE パネル（HUD）と上段デッキの両方にぶつかりました。
2つ手を入れています。

1. **メーター類を盤面の左右へ逃がす**。JP・倍率・STEP・保留を上下に積むのをやめ、
   3x3 の左右にできた余白に置いた。9マス並べたわりに背の伸びは 1.0 で済んでいる
2. **PAY TABLE を画面右の縦帯へ移す**。上部中央は動いている盤面（液晶）に譲る。
   配当表は「見に行く情報」で、優先度が一段低い

実測（900x430）で液晶は 上端 23px / 下端 162px。

#### 【実測】縦に積んだ配当表は画面の高さに依存する

右の縦帯へ移した配当表は、上を STATS パネル（16..99）、下を操作説明バー
（画面下端から 62px）に挟まれます。最初の実装は `top:112px` 固定・高さ 311px で、
**900x430 では下端が操作説明バーと SOUND ON ボタンの下に潜り、
青7(JP) 行と保留ランプが完全に隠れました。**

保留ランプは「いま何回ぶん抽選が溜まっているか」という機能情報で、
隠れると「回さずに溜める」（§3.2）の判断材料が消えます。高さ2段で詰めています。

| 画面高 | 配当表 | 操作説明バー上端 | 余白 |
|---|---|---|---|
| 720px | 112..396 | 658 | 262px |
| 430px | 100..340（段1: 全体を詰める） | 368 | 28px |
| 390px | 100..314（段2: 早見図を落とす） | 328 | 14px |

落とす順番は「早見図 → なし」。5本のラインがどこかは一度覚えれば済みますが、
保留ランプと配当の数字は毎回見るものなので最後まで残します。

#### 結果表示の格は枚数ではなく tier で決める

配当を約2/3に削ったので、`amount >= 60` のような固定閾値をそのまま残すと
**赤7の85%（1ライン=44枚）が中位役と同じ 1400ms の扱いで落ちます。**
一方リーチの引っ張りは `tier === 'big'` を見て最長 1900ms を使うので、
「最長で煽ってから中位役の扱いで終わる」ちぐはぐが 200回に1回起きていました。

```js
const big = tier === 'big' || res.amount >= 40;   // 格 or 枚数が伸びた回
```

枚数側の閾値（60 → 40 / 150 → 100 / 12 → 8）は pay と同じ比率で下げてあるので、
スイカ以下の体感は旧構成と変わりません。バーと赤7だけが `tier` で拾われて
大当たり扱いになり、これは演出の引っ張りと一致させるための意図的な変更です。

#### 点滅のための再描画は 18Hz に間引く

揃ったラインの点滅を `anyBlink` に足したとき、当たり（全回転の38.7%）のたびに
結果表示中ずっと **1024x501 のキャンバス全面再描画とテクスチャ再アップロードが
毎フレーム走る**状態になっていました。位相は実時間で進めたまま、
`_dirty` を立てるのだけをリールの回転と同じ間隔に間引いています。

実測: 当たり表示中 60回/秒 → **15回/秒**、待機中は 0回/秒（変化なし）。

### 3.11 3段クルーン — 待っている時間を商品にする役物

**抽選ボールが払い出し口に落ちるたびに、その球を筐体前面の役物へ持ち上げ、
3枚の皿を順に通します。皿の底には穴が5つあり、そのうち1つ（金の縁）が当たり。
3枚とも当たり穴を引ければ固定 300枚。**

トリガーはフィーバーの STEP と同じ「ボールの落下」です。1回の落下で
STEP が1つ進み、同時にクルーンが1回まわります（サイドポケットに落ちたぶんは対象外）。

> **§7 で「採らない」と書いた案です。**
> 理由は「ボールを別空間へ運ぶ機構が要る／カメラワークが要る」でした。
> 前者は**そのまま見せる**ことで解決しています（リフトの管を筐体の左前に立てて、
> 落ちた球が持ち上がるところを見せる）。後者は**カメラを動かさない**ことで解決しました。
> 役物を常設して既定の画角に収めてあるので、演出のためのカメラ移動はありません。
> ボールは 20秒に1回くらい落ちるので、そのたびに視点が飛ぶ台は成立しません。

#### 置ける場所は1か所しか無かった

| 案 | 測ってみた結果 |
|---|---|
| 盤面の上（上段デッキの上） | 投入したメダルの落下経路（x±5.5, z=-0.6）と当たる |
| 筐体の上（背面壁の上） | 既定のカメラの上端は前面で y≒9.4。y=12 より上は**画面に映らない** |
| 盤面の手前（払い出し口の上） | いちばん見たい「押し出されて落ちる瞬間」が隠れる |
| **液晶の左、ガラスより手前** | **○**（皿の半径ぶん z を前に出してガラス z≦6.55 を避ける） |

さらに HUD との取り合いがあり、左上（持ち枚数）と左下（ITEM）に挟まれた
**縦 3.6 unit 相当の帯**にしか入りません。皿の直径 3.7 のままでは
上段が持ち枚数パネルの裏に隠れたので、役物全体を **0.72倍**に縮めました。

#### 皿の形を作り直した — 回らないクルーンは抽選ではない

最初は「中心へ単調に下る椀 + 中心に当たり穴 + その手前にハズレ穴3つ」でした。
球は回らずに一直線で中心へ向かうので、**投入角と穴の向きの関係だけで結果が決まり**、
段ごとの通過率が 100% / 47% / 100%（各40回）という固定値になりました。

実機のクルーンは「皿の底に穴が並んでいて、そのうち1つが当たり」という形で、
球はその底を何周も回ります。**待っている時間そのものが商品**なのだから、
回らない形にした時点で間違っていました。いまは

- 断面は「谷が円周状にある椀」（中心はむしろ山）
- 穴はその谷の上に5つ、等間隔
- 球は外周から入り、谷へ落ちながら何周も回って、どれかの穴に落ちる

という形です。回っている時間は**中央値 2.1秒 / 95%点 4.7秒 / 最長 7.3秒**（12000回）。

#### 【実測】皿の上だけ Rapier を使っていない

皿を三角形メッシュのコライダーにして Rapier の剛体を転がす、を最初にやりました。
半径 0.27 の球（1 unit = 25mm なので実寸 13mm 相当）では、どう振っても駄目でした。

| 皿の作り | 起きたこと |
|---|---|
| 細かい分割（稜線 0.1）| 稜線に当たるたびに減速。**0.35秒で 9.7 → 0.7 unit/s**。1周もしない |
| 細かい分割 + `FIX_INTERNAL_EDGES` | 減速は消えるが、**1ステップの移動量が「速度×dt」の3〜6割**しかなくなる。速度だけ増えて位置が付いてこない（静止状態から落としても再現）|
| 粗い分割（稜線 0.35）| 稜線で弾かれて**皿の外まで飛ぶ**（半径 3.6 の位置まで到達）|

同じ球を**平らな三角形2枚**の上で転がすと完璧に転がるので、
「細かく曲がった面の上の小さな球」が Rapier の接触計算の想定から外れています。
単位系が実寸の 1/25 で、Rapier が既定で想定する寸法よりかなり小さいのも
効いていると思われます（`lengthUnit` は台全体の挙動に効くので触っていません）。

皿は回転体で、断面 `y = f(r)` が式で書けます。**式が閉じている面の上を転がすだけ**なら
自前の積分のほうが正確で、詰まりも吹っ飛びも起きません。
転がる球の慣性（斜面方向の加速度が 5/7）、転がり抵抗、外周の反発、
穴に掛かってからの落下、までを 40行ほどで書いています（`game/Kuruun.js`）。

**抽選そのものは相変わらず物理です。** 乱数は「皿に放つときの初速と角度のばらつき」
にしか使っていません。結果を先に引いてから演出を合わせる、はしていません
（ばらつきが無いと、決定的なぶん毎回まったく同じ穴に落ちます）。

#### 当選率は「穴の数」ではなく「投入口との位置関係」で決まっていた

穴が5つなら 1/5、と思って作ると外れます。実測（1皿あたり）:

- **穴の並びを絶対角で置くと、段ごとに当選率が変わる**
  段ごとに投入角が違うので、投入口と当たり穴の位置関係も段ごとに変わります。
  実測 10.4% / 23.1% / 25.0%。**穴の向きは投入角からの相対**にして揃えました。
- **当たり穴だけ小さくしてはいけない**
  0.30 と 0.32（6%差）を付けたら、当たり穴に入る割合が **20% → 7%**。
  縁で弾かれた球はそのまま次の穴に落ちるので、
  「わずかに入りにくい穴」は素通りされる穴になります。いまは全部同じ大きさです。
- **転がり抵抗を下げるとばらける**
  周回数が増えるぶん「どの穴に落ちるか」が均されます。
  1皿あたりの当選率の振れ幅が `damp=0.35` で 9〜33%、`damp=0.20` で 15〜26%。
  ついでに回っている時間も 1.4秒 → 1.9秒 に伸びるので、0.20 を採りました。

最終的な位置（穴の間隔 2π/5 に対する回転量）は、20% にいちばん近い 0.42 を実測で選びました。

#### 実測（12000回・早送り）

| 段 | 入った | 抜けた | 通過率 |
|---|---|---|---|
| 1段目 | 12000 | 2473 | **20.6%** |
| 2段目 | 2473 | 503 | **20.3%** |
| 3段目 | 503 | 89 | **17.7%** |
| | | **3段抜け 89回** | **1/135** |

別の 8000回では 1段あたり 20.4 / 19.3 / 20.7%、3段抜け 65回 = 1/123 でした。
**おおむね 1/130 前後**と見ておけば足ります。

ボールの落下は 300秒あたり 12〜19回（§ボールの config）なので、
**300枚が出るのはおよそ 40〜55分に1回**、平均すると 5〜7枚/分の供給になります。
§4.2 の見立て（ホッパーが吐く枚数 6枚/分でおよそ払い戻し +6pt）だと、
通常時の払い戻しに +5〜7pt ぶん乗る計算です。

穴を4つにすると 1段あたり約 27%、3段抜けは **1/53**（15分に1回）まで跳ね上がり、
払い戻しが +15pt を超えます。**当選率の設定は `hole.count` 一箇所**なので、
出方を変えたいときはここを動かして測り直してください。

#### 払い出しは他の当たりと同じ

300枚は**ホッパーから場に降ります**。降ったメダルは山に積まれ、押し出され、
その一部が払い出し口から落ちて初めてクレジットになります（§4.2）。
クルーンだけ直接クレジットに足すと、この台で唯一「押し出しを経由しない払い出し」に
なってしまうので、そこは揃えてあります。
---

---

## 4. 経済（実測）

### 4.1 拡張前の問題は「平坦すぎる」ことだった

| 源 | 払い戻し寄与 |
|---|---|
| プッシャー | 83〜95% |
| スロット | 約 8% |
| **合計** | **90〜103%** |

ほとんど減らず、ほとんど増えない。300枚から始めて300枚のまま20分が過ぎます。

### 4.2 実装後（265秒 × 平衡状態・0.55秒に1枚投入）

| 指標 | 実測 | 備考 |
|---|---|---|
| **払い戻し（場外へ出た枚数 ÷ 投入）** | **76%** | 改修前 71%。長期ではもう少し上（下記）|
| スロットの期待値 | **1.70枚/回** | 1回まわすのにメダル1枚を飲まれる |
| 場内の平衡枚数 | 174〜220 | AntiJam の発動 **0回** |
| ロスト | 払い出しの 15% | JP の主燃料 |
| 演出の長さ | 平均 3.6秒/回 | 処理能力 17回/分 |
| フィーバー | +71枚 / 回 | 25秒で通常の 2.7倍 |
| ジャックポット | 450〜550枚 / 約13分 | |

**通常時が 80% を切っているのが正しい状態です。**
遊んでいるだけではじりじり減るからフィーバーが嬉しく、
減っているから JP メーターを眺める意味が出ます。

#### 【実測・訂正】「ホッパーが吐いた枚数」は払い戻しではない

途中まで払い戻しを `(場外へ出た枚数 + スロットの払い出し) ÷ 投入` で測っていて、
151% だの 193% だのという数字を出していました。**これは間違いです。**

スロットが当たって出るメダルは**ホッパーから場に落ちるだけ**で、
そのままプレイヤーの物になるわけではありません。場に積まれ、押し出され、
その一部がまた払い出し口から落ちて初めてクレジットになります。
正しい払い戻しは **場外へ払い出された枚数 ÷ 投入枚数** の一本です。

この誤りが厄介なのは、**配当を上げると払い戻しが期待値より速く伸びる**点です。
吐いたメダルが場を経由して戻ってくるぶんが効きます。実測の内訳
（ロスト 9〜22% / チャッカー 17〜21%）から解くと:

| スロットの期待値 | 払い戻し | |
|---|---|---|
| 0.94枚 | 約 71% | 改修前。じりじり減る |
| **1.70枚** | **76〜86%** | **採用。** ゆっくり減る |
| 2.00枚 | 約 92% | ほぼ減らない |
| 2.70枚 | 約 113% | 遊んでいるだけで増え続ける = 緊張が消える |

チャッカーに入ったメダルはクレジットになりません。
「チャッカーはメダルを増やす場所ではなく、JP への切符を買う場所」
── 増やすのはプッシャー、賭けるのはチャッカー、という役割分担は変えていません。

### 4.3 配当表（1ライン時代の実装値）

> **この表は 3x3 化する前の値です。** 現在の実装値（1ラインあたりの pay と
> ライン数の重み）は §3.10 の表を見てください。ここは「なぜ配当を引き上げたか」の
> 経緯として残してあります。

**当たりの手応えを作るために、配当を約1.8倍に引き上げました。**
増やしたぶんはベル以上に寄せてあり、上位役ほど倍率が大きくなっています。

| 絵柄 | 重み(/1000) | 改修前 | **実装値** | ×5 のとき |
|---|---|---|---|---|
| ブランク | 640 | 0 | 0 | — |
| リプレイ ⟳ | 165 | 1 | 1 | 5 |
| チェリー 🍒 | 100 | 2 | 2 | 10 |
| ベル 🔔 | 62 | 3 | **6** (2.0倍) | 30 |
| スイカ 🍉 | 18 | 7 | **18** (2.6倍) | 90 |
| バー BAR | 8 | 18 | **40** (2.2倍) | 200 |
| 赤7 | 4 | 30 | **80** (2.7倍) | 400 |
| 青7 | 3 | JP当選 | JP当選 | — |
| | | 期待値 0.94 | **期待値 1.70** (1.8倍) | |

リプレイ(1枚)とチェリー(2枚)は据え置きです。ここは「飲まれた1枚が戻ってくるだけ」で、
当たりの手応えは中位以上で作ります。実機の配当（スピンフィーバーは1回 15〜50枚）も
小刻みに返すのではなく「たまに、まとまって」返す形になっています。

青7を「JPチャンス」に振り替えたのが要点です。
**最上位の役を固定枚数にしておくと、JPメーターを見る理由が消えます。**

#### 【実測】回転数を穴の深さで落とすのは無理だった

当初は「配当4倍・回転半分」で総量を保つつもりで、START の穴の
奥行きを 1.10 → 0.95 に浅くしました。結果は **0.5回/分**。ほぼ全部が橋を架けて跨ぎます。
メダル直径 1.0 のすぐ下は崖になっていて、間の値が取れません（§3.1 の表と同じ現象）。
回転数は 21回/分 のまま据え置き、増やすぶんは全部配当に乗せています。

---

## 5. 性能（実測）

`DESIGN.md §7.6` の実測（200枚→5.1ms / 300枚→10.2ms / 400枚→16.2ms / 500枚→24.4ms）に対して。

| 状態 | 場内 | 1ステップ |
|---|---|---|
| 通常プレイ（平衡）| **205〜250** | 5〜7 ms |
| フィーバー中 | 165〜205 | 同程度 |
| **JP のタワー建設〜払い出し** | **390〜403** | **約 15 ms** |

危ないのはタワーだけで、そこも `towerLimit` と `stallOverflow` で頭を押さえてあります。

追加した剛体はキネマティック5個（プッシャー / 手前テーブル / シャッター2 / フリッパー2）と
Fixed 1個（払い出しスロープ）だけで、いずれもコストは測定できる範囲では出ていません。
チャッカーの判定は既存の走査に条件を足しただけなのでゼロです。

---

## 6. ファイル構成（追加分）

```
src/
├── config.js                 chucker → chute / fever / jackpot / flipper / bump / special / kuruun を追加
├── game/
│   ├── PayoutChute.js   新規  払い出しスロープ + チャッカーの穴 + フリッパー
│   ├── TiltTable.js     新規  手前テーブル（キネマティック化。フィーバーで傾く）
│   ├── Shutters.js      新規  サイドポケットのシャッター
│   ├── FeverMode.js     新規  STEP とフィーバーの状態機械
│   ├── Jackpot.js       新規  JP メーター（演出も払い出しも持たない）
│   ├── JackpotShow.js   新規  park → build → sweep → pay の演出
│   ├── SpecialMedals.js 新規  特殊メダルの在庫・投入・起爆
│   ├── Bump.js          新規  台パンと TILT
│   ├── Kuruun.js        新規  3段クルーン。皿の生成・球の積分・段の進行 (§3.11)
│   ├── LotteryBall.js   改修  落ちた位置を返す（クルーンのリフトが拾う位置）
│   ├── Pusher.js        改修  位相を絶対時刻から積分へ / hold・run モード
│   ├── MedalPool.js     改修  kind と instanceColor
│   ├── Payout.js        改修  3分類（払い出し / ロスト / チャッカー）
│   ├── SlotLines.js     新規  5本のペイラインと、結果から盤面を組む処理 (§3.10)
│   ├── SlotMachine.js   改修  保留オッズ・JP役・ライン数の抽選
│   ├── SlotDisplay.js   改修  3x3 の盤面 / JP・STEP・倍率の常時表示
│   └── Hopper.js        改修  stallOverflow
├── render/
│   └── SymbolArt.js     新規  絵柄の SVG パス。Canvas と React が同じデータを読む
└── ui/
    ├── components/ToolsPanel.tsx   新規  特殊メダルと台パンの状態
    ├── components/SymbolIcon.tsx   新規  絵柄を描く <svg>
    └── store.ts                    改修  ギミック用のフィールドを追加
```

配線はすべて `main.js` に集約してあり、各ギミックはお互いを知りません。

---

## 7. 検討して採らなかった案

| 案 | 採らない理由 |
|---|---|
| ~~**クルーン**（穴の開いた回転皿）~~ | ~~ボールをフィールドから持ち上げて別空間へ運ぶ機構が要る。実質もう1つの物理空間とカメラワークが必要で、フィーバーに比べて費用対効果が悪い~~ → **後から実装しました（§3.11）**。運ぶ機構は隠さずに見せ、カメラは動かさないことで両方とも消えています |
| **ベルトコンベア／風車** | Rapier で表面速度を出すには回転体か接触点への速度注入が必要で、見た目の派手さの割に体験が薄い。プッシャーで既に「動く床」はある |
| **ビンゴ／すごろく** | 液晶は1枚しかなく（§7.9）、HUD も混んでいる。**表示場所が無いギミックは入れない** |
| **上下2段からの投入・発射角度調節** | 上段デッキは「浅く保つ」ことが輸送機構の前提（§2.4）。投入機構を増やすと一番壊れやすい部分を触ることになる |
| **アウトブロック／バンパー盤面** | 盤面をもう1つ作ることになる。こちらのフィーバーは「本物のメダルの山が本当に崩れる」ほうが強い |
| **ミッション／ノルマ制** | ローグライク方向。まず台としての面白さを作ってから |

---

## 8. 設計時の見積りと実測の答え合わせ

当初の §8 に「未検証の前提」として並べた7項目の結果です。**4つ外れました。**

| # | 設計時の見積り | 実測 | |
|---|---|---|---|
| 1 | チャッカーの入賞率は切り欠きの**幅**にほぼ比例する | **外れ。** 効くのは**奥行き**で、メダル直径 1.0 をまたぐと桁が変わる | ✗ |
| 2 | 前縁の切り欠きは詰まりを起こさない | **外れ。** 力の連鎖が切れて機械が止まる。場内 410枚・AntiJam 4回/60秒 | ✗ |
| 3 | キネマティックな `tableFront` を山の下で回しても暴れない | 当たり。1.2秒かければ問題なし | ✓ |
| 4 | `period` を変えてもプッシャーが飛ばない | 位相を積分に変えれば当たり。変えなければ飛ぶ | ✓ |
| 5 | フィーバー25秒で場内が空にならない | 当たり。205 → 165 で、補給（150 以下）まで届かなかった | ✓ |
| 6 | 36層のメダルタワーは**たぶん自立しない** | 当たり。3×3 × 20層の束に変更 | ✓ |
| 7 | 通常時 RTP 70% でフィーバーまで到達できる | 未確認（長時間プレイの実測が要る） | — |

加えて、設計時にまったく想定していなかった事故が2つ:

- **盤面に常設した板は、流れと平行でも機械を止める**（払い戻し 75% → 15%）
- **ホッパーの「詰まり時に上限を無視」が JP で暴発する**（場内 581枚 = プール上限）

`DESIGN.md §9` の「直感が当たらない」という教訓は、拡張でもそのまま当てはまりました。
**この台は、盤面の手前側に物を置くと必ず何かが止まります。**

---

## 9. 残っている宿題

- **長時間プレイの実測**（§8-7）。300枚スタートでフィーバーに1度も入れずに終わらないか。
  入れないようなら `fever.stepsToEnter` を 3 → 2 に下げる
- **CHANCE チャッカーの入賞率**。中央だけを狙うプレイでは 0回/分 になる。
  「狙わないと当たらない」のは正しいが、存在に気づけるかは要確認
- **フィーバー中の補給の閾値**（`refillBelow: 150`）。今回の実測では一度も発動していない
- 効果音のバランス。ギミックが増えたぶん、鳴る音の種類が一気に増えた

---

## 参考にした資料

- [プッシャーゲーム — Wikipedia](https://ja.wikipedia.org/wiki/%E3%83%97%E3%83%83%E3%82%B7%E3%83%A3%E3%83%BC%E3%82%B2%E3%83%BC%E3%83%A0)
- [スピンフィーバー — Wikipedia](https://ja.wikipedia.org/wiki/%E3%82%B9%E3%83%94%E3%83%B3%E3%83%95%E3%82%A3%E3%83%BC%E3%83%90%E3%83%BC)（チェッカー／保留10／倍率／フリッパー／ステップアップ）
- [スマッシュスタジアム — Wikipedia](https://ja.wikipedia.org/wiki/%E3%82%B9%E3%83%9E%E3%83%83%E3%82%B7%E3%83%A5%E3%82%B9%E3%82%BF%E3%82%B8%E3%82%A2%E3%83%A0)（オッズ5段階／プログレッシブJPの加算レート）
- [バベルのメダルタワー — Wikipedia](https://ja.wikipedia.org/wiki/%E3%83%90%E3%83%99%E3%83%AB%E3%81%AE%E3%83%A1%E3%83%80%E3%83%AB%E3%82%BF%E3%83%AF%E3%83%BC)（タワーメカ／JPステップ／段階報酬）
- [Coin pusher — Wikipedia](https://en.wikipedia.org/wiki/Coin_pusher)（二段構造／ホッパー循環）
- [メダルゲーム用語集スペシャル](http://medalyougosyu.seesaa.net/)（チャッカー／ドロップ／ステップアップ／フィーバー）
- [スピンフィーバー3 攻略 — メダルゲーム攻略](https://medalgamefan.com/spinfever/spinfever.htm)
- [「クルーン」とは何ですか？ — SANKYO](https://www.sankyo-fever.jp/beginner/qa/pachinko/93/)
- [メダルプッシャーゲームの感想（note）](https://note.com/packing_gom_9den/n/nab64073c6839)
- [Raccoin: Coin Pusher Roguelike — Wikipedia](https://en.wikipedia.org/wiki/Raccoin:_Coin_Pusher_Roguelike)（特殊コイン）
