/**
 * 全パラメータの一元管理。
 *
 * ■ 単位系 (DESIGN.md §3)
 *   1 unit = 25 mm (メダル直径)。時間は実時間 (秒)。
 *   長さの物差しを変えただけなので、重力を換算すれば挙動は実寸と同一になる。
 *     9.81 m/s^2 = 9810 mm/s^2 / 25 mm = 392.4 unit/s^2
 *
 * ■ 座標系
 *   X: 左右 (右が +) / Y: 上 / Z: 奥行き (手前 = プレイヤー側が +)
 */

export const CFG = {
  physics: {
    gravity: -392.4,
    timestep: 1 / 60,
    maxSubSteps: 3,        // 1描画フレームあたりの物理ステップ上限 (死のスパイラル防止)
    solverIterations: 8,   // 積層の安定性に効く。重ければ 4 まで下げる
    ccd: true,
  },

  medal: {
    radius: 0.5,
    thickness: 0.10,       // 実寸1.8mm(=0.072)より厚め。接触が劇的に安定する (DESIGN.md §3.3)
    segments: 24,
    density: 8.0,
    friction: 0.26,        // 低すぎ=滑って山ができない / 高すぎ=固まって崩れない
    restitution: 0.02,     // ほぼ0。跳ねると一気に嘘くさくなる
    linearDamping: 0.25,
    angularDamping: 0.45,
    // 場内の枚数には「流れる窓」がある。落とし口に勾配を付けたぶん山を厚くできる。
    // 詳しくは §7.6 の実測表を参照。
    maxCount: 585,
    color: 0xd9b45c,
    metalness: 0.95,
    roughness: 0.32,
  },

  pusher: {
    // ストロークは払い出し量に直結する最大の要素 (実測: 0.8→ほぼ落ちない / 1.2→出過ぎ)
    strokeHalf: 0.95,      // 片振幅。全ストローク = 1.9 unit = 47 mm
    period: 1.9,           // 秒/往復
    friction: 0.35,
    restitution: 0.0,
  },

  table: {
    friction: 0.32,
    restitution: 0.0,
  },

  /**
   * 筐体レイアウト。box は「中心座標」と「全体サイズ(w,h,d)」で定義する。
   * 下段テーブルの上面が y = 0。上段(プッシャー上面/固定棚)の上面が y = 1.2。
   */
  layout: {
    deckHeight: 1.2,       // 上段(プッシャー上面)と下段の段差

    /* ------------------------------------------------------------------
     * 上段の輸送機構について (実装上いちばん重要な点)
     *
     * プッシャー上面に乗ったメダルは、前進でも後退でもプッシャーと一緒に動く。
     * つまり放っておくと往復するだけで前に進まない。
     * 前に送るには「後退時にメダルを堰き止める固定物」が要る:
     *   後退 → 背面壁に押し当てられたメダルはその場に残る (= プッシャーに対し前進)
     *   前進 → プッシャーと一緒に前へ運ばれる
     * これが1周期ごとに繰り返されて山全体が前進し、前縁からこぼれ落ちる。
     *
     * そのためプッシャーは背面壁の内側まで潜り込む長さにし、
     * 山が常に壁に接するようにしてある。上段に固定棚は置かない
     * (棚は動かないぶんメダルが溜まる一方の死に領域になり、輸送も止まる)。
     * ------------------------------------------------------------------ */

    // --- 下段テーブル ---
    // 手前側(z>3)は幅を絞り、左右に 1.5 unit のサイドポケット(落ちるとロスト)を作る
    //
    // 【奥行きの取り方】奥側 (z<0) は上段デッキの下に隠れている部分なので、
    // ここを伸ばした量がそのまま **下段の見えている面積** になる。
    // 背面壁とプッシャーを同じ量だけ奥へ下げれば上段デッキの奥行きは変わらず、
    // 下段だけが広がる (DESIGN.md §7.10)
    tableMain:  { x: 0, y: -0.5, z: -1.25, w: 14.0, h: 1.0, d: 8.5 },  // z: -5.5 .. 3.0

    // 落とし口の手前は登り勾配。メダルもボールも簡単には落ちなくなる。
    //   rx は X 軸まわりの傾き。負で手前(+Z)側が持ち上がる
    //   y は「後端の天面が y=0 のまま tableMain と繋がる」ように持ち上げた値:
    //     y = -0.5 + (d/2) * sin(-rx) = -0.5 + 1.5 * sin(0.06) = -0.41
    //   前縁(z=6.0)の天面は y ≒ +0.18 まで上がる
    // 傾けすぎると山が縁に届かず払い出しが止まるので、角度は控えめに (§7.6)
    tableFrontTilt: 0.06,
    tableFront: { x: 0, y: -0.41, z: 4.5, w: 11.0, h: 1.0, d: 3.0, rx: -0.06 },

    // --- プッシャー (キネマティック) ---
    // 幅は壁に食い込ませて左右の隙間をなくす (メダルが挟まって固着するのを防ぐ)
    // 奥行きは背面壁に潜り込む長さ。前進端でも背面が壁の内面 z=-3.0 より奥にある
    pusher: { x: 0, y: 0.6, z: -2.3, w: 14.4, h: 1.2, d: 5.0 },
    // 上段の「左右」に立てる壁。プッシャーと一緒に動く。
    // 端に寄ったメダルが下段の手前へ流れてサイドポケットに落ちるのを減らす。
    // 手前(前面)は塞がない。塞ぐと下段にメダルが降りてこなくなり機械が止まる (§7.7)
    //   h 壁の高さ / t 厚み / inset 壁を立てる x 位置 (筐体の内壁は ±7.0)
    pusherSideWall: { h: 0.30, t: 0.25, inset: 6.0 },
    // 前進端: 前面 z=1.15 / 背面 z=-3.85   後退端: 前面 z=-0.75 / 背面 z=-5.75
    // 露出する上段デッキは背面壁の内面 (z=-3.0) から前面まで、実質 2.25〜4.15 unit。
    // 深いデッキはメダルが溜まる一方で下段に降りてこない (実測で確認済み)。
    // 背面壁ごと奥へ下げてあるので、この奥行きは下段を広げる前と同じ。
    // 前進端の前面から落下口(z=6.0)まで 4.85 unit = メダル約5枚分。

    // --- 囲い ---
    // 背面壁は厚いブロック。この中にプッシャーの後端を隠す
    wallBack:  { x: 0,     y: 6.0,  z: -4.25, w: 14.0, h: 12.0, d: 2.5 }, // z: -5.5 .. -3.0
    wallLeft:  { x: -7.25, y: 6.0,  z: 0.5,   w: 0.5,  h: 12.0, d: 12.0 },
    wallRight: { x: 7.25,  y: 6.0,  z: 0.5,   w: 0.5,  h: 12.0, d: 12.0 },
    glass:     { x: 0,     y: 6.75, z: 6.4,   w: 14.0, h: 10.5, d: 0.3 }, // y: 1.5..12 (下は開口=払い出し口)
    ceiling:   { x: 0,     y: 16.0, z: 0.5,   w: 15.0, h: 0.5,  d: 12.0 }, // 不可視。脱走防止

    // --- メダルを湧かせる既定の位置 ---
    // プレイヤーの投入は発射式に変えたので (CFG.launcher / §7.2)、ここを通るのは
    // デバッグの一括投入だけ。落下高さを上げると 1ステップの移動量がメダル厚を
    // 大きく超え、CCD があっても着弾時に一時的にめり込む (見た目に響く)
    spawn: { y: 7.0, z: -1.6, xLimit: 5.5 },

    // --- 判定しきい値 (DESIGN.md §7.3) ---
    // テーブル下面が y=-1。ここを下回ったメダルは場外へ抜けたものとして分類する
    fallY: -2.0,
    pocketX: 5.5,          // |x| がこれを超えていればサイドポケット経由 = ロスト
    // サイドポケットは z 3.0〜6.0 にしかない。この先 (払い出しスロープ) は
    // 端まで行ってもロストではないので、x だけで判定しないこと
    pocketZEnd: 6.2,
    despawnY: -14.0,
  },

  /**
   * 払い出しスロープとチャッカー (DESIGN_GIMMICKS.md §3.1)
   *
   * 前縁 (z=6.0) を越えたメダルを受ける、手前に下る坂。
   * この坂の上に「穴」を開けたものがチャッカーで、落ちたメダルは飲まれる代わりに
   * 抽選が回る。投入位置が初めて結果に効くようになる、という一点のために入れている。
   *
   * ■ 【重要・実測】穴は「テーブルの上」に開けてはいけない
   *   設計時は手前テーブルの前縁に切り欠きを作る案だった (§3.1 の Phase A 版)。
   *   実装して測ったら、これが機械を止めた。
   *
   *     場内の平衡枚数  231 → 350〜410 まで膨張
   *     払い出し        109枚投入 / 60秒に対して 12枚
   *     AntiJam の発動  60秒で 4回
   *
   *   理由は DESIGN.md §2.4 が上段について書いているのと同じ:
   *   **山は力の連鎖で前に進むので、途中に穴があると連鎖が切れる。**
   *   前縁の穴は「単独で滑ってくる1枚だけが落ちる」つもりだったが、
   *   実際には穴は山の中にある。そこでメダルが消えるぶん、
   *   前縁まで力が伝わらなくなって払い出しそのものが止まった。
   *
   *   実機 (スピンフィーバー) がチェッカーを盤面ではなく
   *   「払い出し口の傾斜」に置いているのは、たぶんこれが理由。
   *   **既に払い出されたメダルを選り分ける**なら、山の力学に一切触れない。
   *
   * ■ 穴の「奥行き」が入賞率を決める
   *   メダルは直径 1.0 なので、坂に開けた穴の長さが 1.0 未満だと橋を架けて跨ぐ。
   *     start  … 1.10 (> 1.0)。帯に来たメダルはほぼ確実に落ちる = 抽選の主動力
   *     chance … 0.70 (< 1.0)。ふつうは跨ぐ。姿勢が崩れた時だけ入る = ボーナス的
   *   幅は「メダルが挟まらない」ための下限 (直径 + 0.2) として効く。
   *
   * ■ 入ったメダルは飲まれる (クレジットにならない)
   *   実機のチャッカーも機内のホッパーに落ちるので払い出されない。
   *   「1枚差し出して抽選を1回買う」という取引にしておかないと、
   *   チャッカーを狙うことが一方的に得になってゲームにならない。
   */
  chute: {
    // 坂そのもの。rx は X 軸まわりの傾き (正で手前が下がる)
    // 天面は (z=6.05, y=-0.30) から (z=8.24, y=-1.50) まで。約 28.7°。
    // メダルの静止摩擦 0.26 は 14.6° 相当なので、確実に滑り落ちる
    box: { x: 0, y: -1.16, z: 7.0, w: 11.5, h: 0.6, d: 2.5, rx: 0.5 },
    // 判定境界 (world z)。これ未満で落ちたらチャッカー、これ以降なら払い出し。
    // 穴を抜けたメダルは z<7.9 で、坂の先端から落ちたメダルは z>8.3 で
    // y=fallY を跨ぐので、間に十分な隙間がある
    splitZ: 8.05,
    slots: [
      // dz は坂のローカル z (坂の中心が 0、手前が +)。
      // kind: 'start'  スロットを1回まわす
      //       'chance' フリッパーが開く + JP メーターに加算
      { id: 'start',   kind: 'start',  x0: -0.55, x1: 0.55,  dz0: -0.55, dz1: 0.55 },
      { id: 'chanceL', kind: 'chance', x0: -3.55, x1: -2.45, dz0: -0.15, dz1: 0.55 },
      { id: 'chanceR', kind: 'chance', x0: 2.45,  x1: 3.55,  dz0: -0.15, dz1: 0.55 },
    ],
  },

  /**
   * 抽選ボール。
   * フィールドに1個だけ転がっている大きな球で、メダルと一緒に押し出される。
   * 手前の払い出し口に落ちるとスロットが回り、少し待って上段に戻ってくる。
   * サイドポケットに落ちた場合はハズレ扱い（抽選なしで戻る）。
   */
  ball: {
    count: 3,              // 常にフィールドに居る数
    radius: 0.65,          // サイドポケットの幅 1.5 より小さくすること (挟まると詰む)
    density: 2.2,          // 質量約2.5。メダル1枚(0.63)の4倍で、山に流されすぎない
    // 摩擦は低め。高いとメダルの山に食い込んで止まり、いつまでも落ちてこない
    friction: 0.10,
    restitution: 0.5,
    color: 0x66e0ff,

    spawnY: 8.0,
    // 戻す位置。ここが落下頻度をほぼ決める。300秒あたりの落下回数 (3個での実測):
    //   z=-0.8 → 4回。奥すぎると山に埋もれたまま出てこない (詰まり検出が29回も走った)
    //   z= 0.0 → 9回   z= 0.6 → 12〜19回   z= 1.2 → 14〜17回
    // 3個あるぶん1個あたりの周期は長くてよいので、デッキの中ほどまで奥に戻す。
    // ※ 上の実測値は上段デッキを奥へ 1.0 下げる前の座標。デッキ上の同じ場所を
    //   指すよう、まとめて -1.0 した値が今の設定 (0.6 → -0.4)
    spawnZ: -0.4,
    spawnXRange: 4.2,
    returnDelay: 1.6,      // 落ちてから戻ってくるまで (秒)

    // 戻った直後の落ち着き。
    // これが無いと、戻ってきた球がそのまま転がって即座に落ち、スロットが連続で回る。
    // この間だけ摩擦を上げて回転と横方向の速度を殺し、その場に着地させる。
    settleSeconds: 2.2,
    settleFriction: 0.95,
    settleRestitution: 0.05,
    settleDamp: 0.5,       // 1ステップごとに横方向の速度に掛ける係数

    stuckSeconds: 10,      // これだけ動かなければ小突いて復帰させる
    stuckNudge: 3.0,
  },

  /**
   * 3段クルーン (DESIGN_GIMMICKS.md §3.11)
   *
   * 抽選ボールが払い出し口に落ちたら、その球を筐体前面の役物まで持ち上げ、
   * 3枚の皿を順に通す。皿の底には穴が hole.count 個あり、そのうち1つ (金の縁) が
   * 当たり。当たり穴に入れば次の段へ落ち、ハズレ穴ならそこで終わり。
   * 3枚とも抜けると固定 pay 枚。
   *
   * ■ 抽選をしていない
   *   どの穴に入るかは、皿の上を転がる球を毎ステップ見て決めている。
   *   乱数を引いてから演出を合わせる、というスロット (§3.9) とは逆の作りで、
   *   乱数は「皿に放つときの初速と角度のばらつき」にしか使っていない。
   *   クルーンは球の行方を見ていること自体が商品なので、
   *   結果を先に決めた時点で、待っている数秒が嘘になる。
   *
   * ■ 当選率は数えて決まるものではなく、測って決まるもの
   *   穴が5つでも 1/5 にはならない。投入口と当たり穴の位置関係、球の周回数、
   *   穴の大きさの差がそのまま効く。実測で 1皿 15〜26% の幅があった。
   *   ここの数字を動かしたら、必ずデバッグパネルの「早送りで当選率を測る」で
   *   測り直すこと (1000回が一瞬で終わる)。
   *
   * ■ 置き場所は筐体の前面 (ガラスの手前) しかなかった
   *   盤面の上に置くと投入したメダルの落下経路 (x±5.5, z=-0.6) と当たり、
   *   筐体の上に置くと既定のカメラの画角 (上端は前面で y≒9.4) から外れる。
   *   液晶 (x±3.7, z=6.1) の左、ガラス (z≦6.55) より手前が唯一空いていた場所。
   *   皿の半径ぶん z を前に出さないとガラスと重なるので、z は皿半径 + 余裕を取る。
   *   さらに HUD (左上の持ち枚数 / 左下の ITEM) に挟まれた帯にしか入らないので、
   *   役物全体を実測しながら 0.72倍まで縮めてある。
   */
  kuruun: {
    enabled: true,
    pay: 500,              // 3段目を抜けたときの払い出し (固定)。ホッパーから場に落ちる
    maxQueue: 2,           // 演出中に落ちた球の待ち行列。これを超えたぶんは捨てる

    /*
     * 役物の位置。**筐体の中**、盤面の左手前に吊ってある。
     *
     * 手前 (ガラスの外) に置くと、いちばん見たい払い出し口の手前に大きな物が
     * 居座って邪魔になる。中に入れるなら置ける場所は限られていて、
     * 下の4つをぜんぶ避ける必要がある:
     *
     *   ガラス      z ≦ 6.25。皿の半径 (1.33) ぶん手前に出せない
     *   左の内壁    x ≧ -7.0。支柱ぶん (半径 + 0.24) 内側に収める
     *   ホッパー    x ±5.0 / z 1.05〜1.75 に降ってくる。皿はコライダーを
     *               持たないので、真下に置くとメダルが皿をすり抜けて見える
     *   発射の弾道  発射口 (0, 1.35, 6.15) から扇形に飛ぶ。z=3.3 の高さでも
     *               左右 ±2.4 までしか届かないので、x=-3.3 より外なら当たらない
     *
     * 液晶 (x±3.7, z=6.1) の裏に隠れないよう、皿の上端は液晶の下端 (y=5.19)
     * より下に収めてある。
     */
    x: -5.20,
    z: 4.65,
    // 3段目 (最下段) の皿の底。低くすると奥の山と重なって見え、
    // 上げすぎると1段目が液晶 (下端 y=5.19) の裏に回る
    y0: 1.95,
    tierGap: 1.44,         // 段の間隔。皿の全高 (椀 + 壁) + 球の直径より広く取ること

    /**
     * 皿。断面は「谷が円周状にある椀」で、穴はその谷に等間隔で並べる。
     *
     * ■ 形を作り直した
     *   最初は中心へ単調に下る椀にして「中心が当たり穴、その手前にハズレ穴を3つ」
     *   という形にしていた。球は回らずに一直線で中心へ向かうので、投入角と穴の
     *   向きの関係だけで結果が決まってしまい、段ごとに 100% / 47% / 100% という
     *   固定値になった (40回)。**回らないクルーンは抽選になっていない。**
     *   実機は「皿の底に穴が並んでいて、そのうち1つが当たり」という形で、
     *   球はその底を何周も回る。待っている時間そのものが商品なのだから、
     *   回らない形にした時点で間違っていた。
     *
     * ■ 皿の上だけは Rapier を使っていない (game/Kuruun.js の頭を参照)
     *   三角形メッシュの上で小さな球 (当時 半径 0.27) を転がすと、実測で
     *     ・分割を細かくすると 稜線に当たるたびに減速し、0.35秒で 9.7 → 0.7 unit/s
     *     ・FIX_INTERNAL_EDGES を付けると減速は消えるが、速度どおりに動かなくなる
     *       (1ステップの移動が速度×dt の 3〜6割で、位置と速度が食い違う)
     *     ・分割を粗くすると稜線で弾かれて皿の外まで飛ぶ
     *   の3択だった。皿は回転体で式が閉じているので、ここだけ自前で積分している。
     */
    dish: {
      R: 1.33,             // 皿の外周
      troughR: 0.65,       // いちばん低い円周。穴はこの上に並ぶ
      bowlH: 0.145,        // 外周と谷の高低差。深いほど球が速く谷へ落ちる
      // 外周に立てる壁の高さ。**見た目だけ**の値で、球は R_MAX で止めているので
      // ここを低くしても飛び出さない。高くすると皿が「深いカップ」に見えて、
      // 実機の浅い皿から遠ざかる
      wallH: 0.30,
      wallLean: 0.09,      // 壁の上端を内側に寄せる量。跳ね出しを防ぐ庇になる
      rInner: 0.07,        // 中心の分割の起点 (見た目だけ。蓋を被せる)
      segments: 96,        // 円周方向の分割。穴の縁のギザつきはここで決まる
      rings: 26,           // 半径方向の分割
      // 転がり抵抗 (1/秒)。大きいほど早く谷で力尽きる。
      // 小さいほど周回数が増えて「どの穴に落ちるか」がばらけるので、
      // 当選率が投入角との位置関係に振り回されにくくなる (実測: 1皿あたり
      // 0.35 で 9〜33% / 0.20 で 15〜26%。ついでに回っている時間も 1.4秒 → 1.9秒)
      damp: 0.20,
      wallBounce: 0.45,    // 外周の壁の反発
      minSpeed: 0.76,      // これを下回ったら谷に沿って押す (筐体の微振動に相当)
    },

    /**
     * 穴。谷の上に count 個を等間隔に並べ、そのうち1つ (金の縁) が当たり。
     * 3段抜けの確率は「1皿あたりの当選率」の3乗になる。
     */
    hole: {
      // 1皿あたりの穴の数。**ここが台の設定そのもの**。
      // 4個 (1皿 27% / 3段で 1/53) だと 500枚が 15分に1回まで来てしまい、
      // 払い戻しが跳ね上がる。5個 (1皿 20% / 3段で約 1/130) に落としてある
      count: 5,

      // 穴の大きさ。当たりもハズレも同じにしてあることに意味がある:
      //   ・谷の底で球が桟に乗って止まれない大きさにすること。穴どうしの間隔
      //     (谷の上で 1.06) から穴の直径を引いた残りが球の直径 (0.39) より
      //     狭くないと、桟の上に乗ったまま何十秒も止まる
      //   ・当たり穴だけ小さくしてはいけない。6% 差 (0.30 と 0.32) を付けたら
      //     当たり穴に入る割合が 20% → 7% まで落ちた。縁で弾かれた球はそのまま
      //     次の穴に落ちるので、「わずかに入りにくい穴」は素通りされる穴になる
      win: 0.32,
      lose: 0.32,

      // 支えを失う判定を、穴の半径からこれ×球半径ぶん内側に取る。
      // ここから先は落下として積分するので、速い球は沈みきる前に向こう岸へ届く
      // = 勢いがあると穴を飛び越える
      grip: 0.35,

      /*
       * 穴の並びの向き (rad)。**投入角からの相対**で置くのが要点。
       *
       * 絶対角で置くと、段ごとに投入角が違うぶん「投入口と当たり穴の位置関係」も
       * 変わり、段ごとに当選率が変わってしまう (実測・各500回で
       * 1段目 10.4% / 2段目 23.1% / 3段目 25.0%)。相対にすれば3段とも同じ形になる。
       *
       * 値は実測で選んである (1段目・各2000回。穴の間隔 2π/5 = 1.26 が1周期):
       *   0.00 → 16.9%   0.13 → 23.3%  0.25 → 24.9%  0.38 → 19.1%  0.50 → 22.1%
       *   0.63 → 25.9%   0.75 → 23.6%  0.88 → 14.8%  1.01 → 14.9%  1.13 → 15.1%
       *
       * 穴が5つなら 1/5 = 20% が素直なので、それに近い 0.42 を採る。
       * ここを動かすだけで当選率が 15〜26% の間で動くので、必ず測り直すこと
       */
      offset: 0.42,
    },

    /** 役物の中だけを転がる球。フィールドの抽選ボールとは別物 */
    ball: {
      radius: 0.195,
      // ガラス越しに見る小さな球なので、皿の色に埋もれないよう白寄りにしてある
      color: 0xd8f6ff,
    },

    /** 皿への投入。外周の内側 inset のところに置いて接線方向に蹴り出す */
    entry: {
      inset: 0.09,         // 壁のすぐ内側から回り始める
      // 接線方向の初速 (unit/s)。外周で釣り合う速さより少し遅く入れて、
      // 外周から谷へ落ちていくようにしてある。
      // 皿の寸法を変えたらここも変える: 長さを s 倍したら初速は √s 倍、
      // 転がり抵抗 (dish.damp) は 1/√s 倍にすると軌道が相似になる
      // (実測でも 0.72倍に縮めたとき当選率がほぼ変わらなかった)
      speed: 9.3,
      // 初速のばらつき (±の割合) と投入角のばらつき (±rad/2)。
      // **これが唯一の乱数**で、小さいと物理が決定的なぶん毎回同じ穴に落ちる
      jitter: 0.18,
      angleJitter: 0.30,
      // 段ごとの投入角 (rad)。0 = +x 方向、π/2 = 手前(+z)。
      //   1段目 -π/2 … 奥。リフトを奥側に立てているため。
      //     盤面の内側 (+x 側) に立てると、液晶 (x±3.7, z=6.1) の裏に隠れる
      //   2・3段目 … 手前寄り。次の皿へ落ちるところが見えるように
      // ここを変えると穴の並びも一緒に回る (hole.offset は投入角からの相対) ので、
      // 当選率は変わらない
      angle: [-1.5708, 1.05, 2.09],
    },

    liftSeconds: 1.5,      // 払い出し口からリフトで持ち上がるまで
    transferSeconds: 0.8,  // 当たり穴から次の皿へ
    missSeconds: 0.55,     // ハズレ穴に飲まれて消えるまで
    winSeconds: 2.0,       // 3段目の当たり穴に入ってから払い出しが始まるまで
    restSeconds: 0.6,      // 次の球を受けるまでの間

    // 保険。皿の上でこれだけ経ったらいちばん近い穴に落とす。
    // 谷で力尽きたときは dish.minSpeed が押し続けるので、ここに来ることは無いはず
    forceTimeout: 25,
  },

  /**
   * UFO ボーナス (DESIGN_GIMMICKS.md §3.12)
   *
   * 低確率で盤面の上空に UFO が現れ、**発射したメダルを直接当て続ける**と
   * 固定 100枚。当たり判定も払い出しも他の当たりと同じ道を通る:
   * メダルはコライダーに当たって実際に跳ね返り、100枚はホッパーから場に落ちる。
   *
   * ■ 置き場所は「弾道の上」から決めている
   *   盤面の適当な高さに浮かべると、首振りの端では届かない・中央では通り過ぎる、
   *   という「そもそも当たらない場所」ができてしまう。
   *   UFO は発射口から見た**水平距離を固定**して漂わせてある (game/Ufo.js)。
   *   弾道は首の角度まわりに回転対称なので、水平距離さえ合っていれば
   *   「首が UFO の方を向いた瞬間に撃てば必ず当たる」位置になる。
   *   プレイヤーが読むのは向きだけで済み、飛距離を読む必要が無い。
   *
   * ■ 出現率がそのまま払い戻しになる
   *   期待値は pay x chancePerInsert (投入1枚あたり) で、他に効く要素が無い。
   *   1/2000 なら +5.0 ポイント。ホッパー経由なので実際に手元に戻るのは
   *   その 7割ほど (残りはサイドポケットへ落ちる) で、正味 +3〜4 ポイント。
   *   ここを動かしたら DESIGN_GIMMICKS.md §4.2 の払い戻しを測り直すこと。
   */
  ufo: {
    enabled: true,
    pay: 100,              // 撃ち落としたときの払い出し (固定)。ホッパーから場に落ちる

    // 出現抽選は**メダル1枚の投入ごと**に引く。時間で引くと、手を止めている間に
    // 勝手に出て勝手に去っていく (誰も撃っていない UFO は演出ですらない)
    chancePerInsert: 1 / 2000,
    // 連続で出ないようにする最短間隔 (秒)。抽選は独立なので、
    // 何もしないと 1/2000 が2回続けて当たることがある
    minIntervalSeconds: 120,

    // --- 撃ち落とすまで ---
    // 【実測】押しっぱなしで撃つと 4.1発/秒 当たる (7秒で 28〜29発・51発中)。
    // 12発だと 3秒で終わって取りこぼしようが無く、24発だと滞空時間ぎりぎりになる。
    // 20発 = 約5.2秒で、**気付いて撃ち始めるまでの 1.8秒**が勝敗を分ける
    hits: 20,              // 必要な直撃数。リムのランプの数でもある
    lifeSeconds: 7.0,      // 滞空時間。撃ち落とせなければ去る
    // 【要点】「当て続ける」ようにするための減衰。
    // これだけ当たらない時間が続くとダメージが1つ戻る。
    // 合計で数えるだけだと、当たるまで延々と撃つのと変わらない。
    // 押しっぱなしで撃っている間は、首が UFO から離れている谷でも
    // 命中の間隔は最大 1.0秒ほど (実測) なので、撃ち続けている限りは戻らない
    decaySeconds: 1.2,
    // 命中と数えるメダルの水平速度 (unit/s)。
    // ホッパーから降ってくるメダルが UFO をかすめても当たりにしないための足切り。
    // 撃ち出したメダルは頂点でも水平 30 前後あるので、まず落ちない
    minHitSpeed: 15,

    // --- 浮かぶ位置 ---
    // 発射口から着地点までの水平距離に対する割合。0.46 は弾道の頂点のあたりで、
    // 上段デッキから 3.8 ほど上、ホッパーの落下位置 (z 0.05〜0.75) の手前
    arcFrac: 0.46,
    driftYaw: 0.62,        // 左右の漂い幅。首振りの片振幅に対する割合
    driftPeriod: 5.3,      // 漂いの周期 (秒)。首振り (2.4秒) と割り切れない値にする
    bob: 0.13,             // 上下の揺れ幅
    bobPeriod: 1.9,

    // --- 寸法と当たり方 ---
    radius: 1.10,          // 円盤の半径。コライダー (円柱) もこの半径
    height: 0.44,
    hitMargin: 0.10,       // 判定はコライダー + メダルの半径 + この余裕
    restitution: 0.55,     // よく跳ね返るほうが当たったことが分かる
    friction: 0.08,        // 上面に乗って居座らないよう、よく滑らせる

    enterSeconds: 0.55,    // 現れるまで
    winSeconds: 1.1,       // 撃ち落としてから去り始めるまで
    leaveSeconds: 0.9,     // 去るまで
  },

  /**
   * スロット。絵柄は8種類、3列 x 3段の盤面で 5本のペイラインを見る。
   * 絵柄の構成と出現比率は下記を参考にしている:
   *   https://qiita.com/hunyu/items/4547d1d9fb734d81d189
   *
   * ただし配当枚数はこの筐体が捌ける量に縮めてある。
   * 元記事は バー150 / 赤7:350 / 青7:711 枚だが、場内の上限が 195 枚なので、
   * そのまま採用すると放出が10分以上終わらない (§7.6 の実測を参照)。
   *
   * ハズレ(blank)の比率も元記事の 77% から 61% に下げている。
   * 元記事は数秒に1回まわす台だが、こちらは「ボールが落ちたとき」しか回らないため、
   * 77% のままだとほとんど何も起きないまま終わってしまう。
   * 当たり絵柄どうしの比率は元記事のまま。
   */
  slot: {
    /*
     * ■ 「小刻みに」から「たまに、大きく」へ振り直した (DESIGN_GIMMICKS.md §4.3)
     *   1回まわすのに START チャッカーがメダル1枚を飲むので、
     *   「回転あたりの期待値 × 回転数」がそのままスロットの払い出しになる。
     *   同じ総量でも、配分の仕方で体感はまったく違う。
     *
     *     旧: 期待値 0.94枚 × 21回/分 = 20枚/分  … ベル3枚、スイカ7枚、バー18枚
     *     中: 期待値 1.70枚 × 21回/分 = 36枚/分  … ベル6枚、スイカ18枚、バー40枚、赤7:80枚
     *     今: 期待値 1.99枚 × 21回/分 = 42枚/分  … 下の symbols を参照 (3x3・5ライン)
     *
     *   ※ 「中」は 1ライン時代の値で、いまの実装値ではない。3x3 化で
     *      1ラインあたりの pay を約2/3に削り、増えたライン数と釣り合わせている。
     *      現行の内訳は symbols の直前の解説と DESIGN_GIMMICKS.md §3.10 にある。
     *
     *   増やしたぶんはベル以上に寄せてある。リプレイ(1枚)とチェリー(2枚)は
     *   「飲まれた1枚が戻ってくるだけ」で、当たりの手応えは中位以上で作る。
     *   実機の配当（スピンフィーバーは1回 15〜50枚）も、小刻みに返すのではなく
     *   「たまに、まとまって」返す形になっている。
     *
     * ■ 【実測】上限は「プレイヤーの払い戻しが 100% を超えないこと」で決まる
     *   ホッパーが吐いたメダルは場に落ちるだけで、そのままプレイヤーの物にはならない。
     *   実際の払い戻しは **場外へ払い出された枚数 ÷ 投入枚数** で、
     *   平衡状態では (投入 + スロット払い出し − ロスト − チャッカー) ÷ 投入 になる。
     *
     *   スロットが吐いた枚数は場に戻り、その一部がまた払い出されるので、
     *   期待値を上げると払い戻しは**期待値より速く**伸びる。
     *   実測の内訳（ロスト 9〜22% / チャッカー 21%）から解くと:
     *
     *     期待値 0.94枚 → 約 71%   じりじり減る（改修前）
     *     期待値 1.70枚 → 約 86%   ゆっくり減る            （1ライン時代の採用値）
     *     期待値 2.00枚 → 約 92%   ほぼ減らない            ← 採用 (実測 1.99枚)
     *     期待値 2.70枚 → 約 123%  遊んでいるだけで増え続ける = 緊張が消える
     *
     *   フィーバーとジャックポットを足して 100% 前後に収まるので、
     *   通常時は 100% を切っているのが正しい。
     *   3x3 化で 86% → 92% に上げてある。「ほぼ減らない」の側に寄せたのは、
     *   ライン数という新しい当たり方を入れたぶん、当たる頻度と額の両方を
     *   わずかに上げたかったため。ここを 90% 未満に戻すのは意図的な巻き戻しになる。
     *
     * ■ 【実測】回転数を穴の深さで落とすのは無理だった
     *   当初は「配当4倍・回転半分」で総量を保つつもりで、START の穴を
     *   奥行き 1.10 → 0.95 に浅くした。結果は **0.5回/分**。ほぼ全部が橋を架けて跨いだ。
     *   メダル直径 1.0 のすぐ下は崖で、間の値が取れない。
     *   回転数は 14回/分 のまま据え置き、増やすぶんは全部配当に乗せている。
     *
     * ■ 総量の上限は「場が捌ける量」で決まる
     *   投入 109枚/分 に対して、場外へ抜けていく量には上限がある。
     *   ホッパーからの払い出しを増やしすぎると場内が膨らみ、最後は詰まる。
     *   期待値 3.8枚（38枚/分）だと投入と合わせて 147枚/分 になり、危ない。
     *
     * 青7だけは枚数を持たない。引くとジャックポット当選 (§3.4) になる。
     * 最上位の役を固定枚数にしておくと JP メーターを見る理由が消えるため。
     */
    /*
     * ■ 3x3 化に伴う配当の振り直し
     *   1ライン時代は「絵柄を1つ引く」だけで終わっていたが、5ラインになると
     *   1回の抽選で複数ラインが同時に成立しうる。pay をそのまま据え置くと
     *   払い戻しがライン数ぶん膨らむので、**pay を約 2/3 に削って**
     *   増えたライン数と釣り合わせてある。
     *
     *   期待値 = Σ(weight/1000 x pay x そのランクの平均ライン数)
     *
     *     リプレイ 172 x  1 x 1.66 =  285
     *     チェリー 108 x  2 x 1.66 =  359
     *     ベル      68 x  4 x 1.48 =  403
     *     スイカ    22 x 12 x 1.48 =  391
     *     バー      10 x 26 x 1.15 =  299
     *     赤7        5 x 44 x 1.15 =  253
     *                              ------
     *                       合計    1990 / 1000 = 1.99 枚/回
     *
     *   旧構成の 1.70 枚 (払い戻し約86%) に対して 1.99 枚 = 約92%。
     *   下の実測表でいう「ほぼ減らない」の手前で、
     *   当たる頻度 (35.7% → 38.5%) と1回の額の両方をわずかに上げている。
     *
     * ■ 1回の最大払い出しは据え置いた
     *   バーと赤7は 3ライン以上を引かない (下の lineWeights を参照)。
     *   赤7 44枚 x 2ライン x 倍率5 = 440枚 で、1ライン時代の
     *   80枚 x 倍率5 = 400枚 とほぼ同じ。ホッパーが捌ける量を超えさせない。
     *
     * tier は「役の格」。配当のライン数抽選 (lineWeights) と
     * 演出の期待度 (show.weights) の両方がこれを見る。
     */
    symbols: [
      { id: 'blank',  name: 'ブランク', color: '#5b6a86', weight: 612, pay: 0,  tier: 'miss' },
      { id: 'replay', name: 'リプレイ', color: '#8fd7ff', weight: 172, pay: 1,  tier: 'small' },
      { id: 'cherry', name: 'チェリー', color: '#ff6b7a', weight: 108, pay: 2,  tier: 'small' },
      { id: 'bell',   name: 'ベル',     color: '#ffd45c', weight: 68,  pay: 4,  tier: 'mid' },
      { id: 'melon',  name: 'スイカ',   color: '#7ee08a', weight: 22,  pay: 12, tier: 'mid' },
      { id: 'bar',    name: 'バー',     color: '#e8eefc', weight: 10,  pay: 26, tier: 'big' },
      { id: 'red7',   name: '赤7',      color: '#ff4d5e', weight: 5,   pay: 44, tier: 'big' },
      { id: 'blue7',  name: '青7',      color: '#5cc8ff', weight: 3,   pay: 0,  tier: 'jp', jp: true },
    ],

    /**
     * ペイライン (DESIGN_GIMMICKS.md §3.10)
     *
     * 盤面は 3列 x 3段。1本のラインを「列ごとに、どの段を見るか」で書く。
     * 3x3 では 横3 + 縦3 + 斜め2 の8本まで取れるが、縦は入れていない。
     * 1列が丸ごと1本のラインになると、リールを1つ止めた瞬間に当たりが
     * 確定してしまい、「3つ目を待つ」というスロットの骨格が壊れるため。
     *
     *   0: 上段    1: 中段    2: 下段    3: 右下がり    4: 右上がり
     */
    lines: [
      [0, 0, 0],
      [1, 1, 1],
      [2, 2, 2],
      [0, 1, 2],
      [2, 1, 0],
    ],

    /**
     * 「何本のラインで揃えるか」の重み。並びは [1本, 2本, 3本, 5本]。
     *
     * 4本が無いのは「狙って作れる形」ではないから。上段と下段を揃えると
     * 四隅が埋まり、そこに斜めを1本足すともう1本の斜めも勝手に成立して4本になる。
     * 横3本を全部揃えれば9マスすべてが同じ絵柄になり、これは5本。
     * どちらも副産物なので、本数として引くのは 1 / 2 / 3 / 5 だけにしてある。
     *
     * 格が上がるほど1本に寄せてある。大きい役まで多ライン化すると
     * 1回の払い出しがホッパーの処理能力を超えるため (上の解説を参照)。
     *
     *   small 平均 1.66本 / mid 平均 1.48本 / big 平均 1.15本
     */
    lineWeights: {
      small: [58, 26, 12, 4],
      mid:   [66, 24, 8, 2],
      big:   [85, 15, 0, 0],
    },
    // 保留が満杯になったあとの入賞は捨てずに倍率へ回す (DESIGN_GIMMICKS.md §3.2)。
    // 「回さずに溜める」という選択が生まれるのが狙い。当選して払い出したら ×1 に戻る
    //
    // 演出は1回およそ4秒 = 毎分15回が処理の上限。保留を深くしすぎると
    // 「倍率が常時最大」になって倍率の意味が消えるので、3 で止めてある
    maxQueue: 3,           // 演出中に溜められる抽選の数
    odds: [1, 2, 3, 5],    // 溢れた入賞がここを1段ずつ上げる
    // 溢れ何回で1段上がるか。1 にすると、チャッカーが演出より速い場面で
    // 倍率が最大に張り付いてしまう (実測: 平均 ×2.78 / 総払い戻し 151%)
    oddsStep: 3,

    /**
     * 演出 (DESIGN_GIMMICKS.md §3.9)
     *
     * 実機の演出は「色で期待度を示す」という一本の約束でできている。
     * 業界共通の並びは 白 < 青 < 黄 < 緑 < 赤 < 金 < 虹 で、虹は大当り濃厚。
     * ここでは 黄 を省いた6段にして、heat 0〜5 に割り当てている。
     *
     * ■ 大事なのは「信頼度が本当に一致していること」
     *   色を賑やかしで振ると、プレイヤーはすぐ「意味が無い」と学習して見なくなる。
     *   そこで **結果を先に決めてから、その結果に応じた重みで heat を引く**。
     *   下の weights から逆算した実際の信頼度は:
     *
     *     heat 0 白  8.6%   演出なし
     *     heat 1 青 36.2%   予告音 + 青枠
     *     heat 2 緑 59.9%   緑枠 + 擬似連1回
     *     heat 3 赤 76.0%   赤枠 + カットイン
     *     heat 4 金 85.7%   金枠 + 擬似連2回 + カットイン
     *     heat 5 虹  100%   虹枠 + フリーズ + カットイン (当たり濃厚)
     *
     *   weights は「役の格 (tier) ごとに、どの heat を出すか」の重み。
     *   格が上がるほど高い heat が出やすいので、金や虹は
     *   「当たる」だけでなく「大きい」ことも示唆する。
     */
    show: {
      /** heat の色。白 < 青 < 緑 < 赤 < 金 < 虹 */
      colors: ['#c8d4e8', '#5cc8ff', '#7ee08a', '#ff4d5e', '#ffcf5c', '#ff9de0'],
      names: ['', 'CHANCE', 'CHANCE', 'HOT!', 'SUPER!', 'PREMIUM'],
      // 役の格ごとの heat 重み。合計は 100 でなくてよい
      weights: {
        miss:  [58, 26, 11, 4, 1, 0],    // ブランク
        small: [12, 30, 30, 20, 8, 0],   // リプレイ / チェリー
        mid:   [4, 18, 30, 30, 15, 3],   // ベル / スイカ
        big:   [0, 5, 15, 30, 35, 15],   // バー / 赤7
        jp:    [0, 0, 0, 10, 30, 60],    // 青7
      },
      // --- 各演出の発生条件と長さ (秒) ---
      noticeMs: 620,        // 予告 (heat>=1)。回す前に色枠を出す
      pseudoMs: 700,        // 擬似連1回ぶん。止まりかけて再始動する
      pseudoAt: [0, 0, 1, 1, 2, 2],   // heat ごとの擬似連の回数
      cutInAt: 3,           // これ以上の heat でカットイン
      cutInMs: 900,
      freezeAt: 5,          // これ以上の heat でフリーズ (全部止まる)
      freezeMs: 1100,
      // 第3リールの「滑り」。当たり絵柄を一瞬見せてから1つずらして止める。
      // ニアミスはハズレの中でいちばん印象に残る瞬間なので、ここだけは必ず見せる
      slipMs: 260,
      reachMs: [900, 1500, 1900],   // リーチの引っ張り: 弱 / 中 / 強 (heat で選ぶ)
    },

    // 演出を描く液晶パネル。画面に被せる DOM ではなく、筐体の前面
    // (ガラスのすぐ内側) に貼った板として 3D シーンの中に置く。
    //
    // 位置決めの制約は上下2つ:
    //   上 … 画面上部の PAY TABLE パネル (HUD) に掛からないこと
    //   下 … 上段デッキに掛からないこと。盤面を隠したら本末転倒
    // カメラに近いぶん見かけが大きくなるので、world 座標の y は控えめでよい
    //
    // 3x3 化で縦に 2.6 → 3.62 と伸びた。メーター類 (JP/倍率/STEP/保留) は
    // 盤面の左右へ逃がしてあるので、9マスを並べたわりに伸びは 1.0 で済んでいる。
    //
    // 【実測 800x450 の画面】上端 27px / 下端 168px。上段デッキの手前の縁が
    // 195px あたりなので、下は 27px の余裕がある。上は配当表 (HUD) を
    // 画面上部中央から右の縦帯へ移して空けた
    display: { x: 0, y: 7.0, z: 6.1, width: 7.4, height: 3.62 },
  },

  /**
   * フィーバー (DESIGN_GIMMICKS.md §3.3)
   *
   * 抽選ボールが払い出し口に落ちるたび STEP が1つ進み、
   * stepsToEnter で突入する。中身はすべて物理側の変更で、液晶の中では起きない。
   *
   *   ① 落とし口の勾配を反転させる … §7.6 で「勾配を付けたら払い戻しが上がった」
   *      と実測した張本人を逆に倒す。山ごと手前に流れる
   *   ② ストロークを拡大する       … §5 の実測表で「出過ぎる」と判定して
   *      通常時には採用しなかった設定を、ここでだけ解禁する
   *   ③ サイドポケットを塞ぐ       … ロスト率 26〜31% がゼロになる
   *   ④ ホッパーが降り続ける       … ①②で場が痩せるので補給する
   */
  fever: {
    stepsToEnter: 3,       // ボール何個ぶんで突入するか
    seconds: 25,
    transition: 1.2,       // 傾斜とストロークが移りきるまで (秒)。速くすると山が暴れる
    tableRx: 0.04,         // フィーバー中の手前テーブルの傾き (正で手前に下る)
    strokeHalf: 1.25,      // 通常 0.95
    period: 1.5,           // 通常 1.9
    closePockets: true,
    // 補給は定量ではなく「場が痩せたら足す」。
    // 定量だと出方の良い日に場内が膨らんで 1ステップが伸びる
    refillBelow: 150,      // 場内がこれを下回ったら
    refillRate: 6,         // 枚/秒 で足す
    /*
     * フィーバー中に液晶へ流す映像 (src/vid/fever_1.mp4)。
     * 液晶は 1024x501 の canvas 1枚で、書き直すたびに全面がテクスチャに
     * 上がる。fps を上げるほどそのまま描画時間になるので、映像が滑らかに
     * 見える下限で止めてある (通常時の点滅は 18Hz で描き直している)。
     */
    video: {
      volume: 0.55,        // 効果音と混ざるので控えめに。master 経由なので消音も効く
      fps: 30,             // フィーバー中に液晶を描き直す回数/秒
      dim: 0.30,           // 映像の上に敷く黒。リールと数字が読めなくなるのを防ぐ
      // 左右の端をさらに落とす幅 (テクスチャのピクセル)。
      // JACKPOT / STEP / HOLD のメーターは盤面の左右の余白に置いてあり、
      // 明るい映像がそのまま来ると読めなくなる。中央の映像は明るいままにしたいので、
      // 全体を暗くするのではなく端だけ落とす
      edge: 300,
      edgeDim: 0.82,
    },
  },

  /**
   * プログレッシブ・ジャックポット (DESIGN_GIMMICKS.md §3.4)
   *
   * サイドポケットのロストを主要な燃料にしているのが要点。
   * 実測でロストは 26〜31% もあるのに、いまはただ減るだけで何も起きない。
   * ここを JP の燃料にすると、いちばん腹が立つ現象がいちばん期待を煽る現象になる。
   */
  jackpot: {
    initial: 200,
    perInsert: 0.05,       // メダル投入1枚あたり
    perLost: 1.0,          // サイドポケットに1枚落ちるたび
    perChance: 1.0,        // チャンスチャッカー1回あたり
    max: 800,              // これ以上は増えない。出し切るのに時間が掛かりすぎる

    // 当選演出でタワーとして積む上限。
    // §7.6 の実測 (400枚→16.2ms / 500枚→24.4ms) から、
    // 発動時の平衡枚数 180 に足して 360枚 ≒ 13ms に収まる量。ここを超えて積まないこと
    towerLimit: 180,
    tower: {
      // 1本柱にすると 180枚 = 18 unit で天井 (y=16) を突き抜ける。
      // 3×3 の束にして 20層 = 2.0 unit の「塔」にする
      cols: 3, rows: 3, spacing: 1.15,
      // 後退したプッシャーの上面 (z: -5.75 .. -0.75) に載せる。
      // ここから外すと、崩しにいく前進で運ばれずに置き去りになる
      x: 0, z: -2.2,
      // 上段には既に山があるので、そのさらに上から落とす。
      // 山に食い込む高さで生成すると剛体が弾け飛ぶ (prefill と同じ理由)
      y0: 2.6,
      rate: 24,            // 枚/秒。速すぎると下が落ち着く前に積んで自壊する
      solverIterations: 14, // 建設中だけ上げる (数秒なので許容できる)
    },
    parkSeconds: 2.0,      // プッシャーを後退させて台を静める時間
    sweepSeconds: 3.0,     // 崩しにいく前進の時間
    // 前進の終端。通常の前進端は z=-0.35 (前面 z=2.15)。
    // ここを深くしすぎるとプッシャーの底面が手前テーブルの天面に食い込むので、
    // 前面が下段テーブルの境目 (z=3.0) で止まる 0.5 までにしてある
    sweepZ: 0.5,
  },

  /**
   * フリッパー (DESIGN_GIMMICKS.md §3.6)
   * チャンスチャッカーを通すと開き、START チャッカーへの流れを作る。
   *
   * ■ 【実測】盤面の上には置けない。払い出しスロープの上なら置ける
   *   最初は手前テーブルの上に、流れと平行に寝かせて常設していた。
   *   「平行なら邪魔しない」という直感は完全に外れて、
   *   払い戻しが 75% → 15%、場内が 368枚まで膨らんだ。
   *   DESIGN.md §7.7 の「手前の壁は機械を止める」と同じで、
   *   払い出し口の手前にある常設の障害物は、向きに関係なく山の流れを殺す。
   *
   *   チャッカーごと払い出しスロープへ移したことで、この問題は消えた。
   *   スロープの上は「もう払い出されたメダルが1枚ずつ滑り落ちてくる」場所で、
   *   山の力学とは無関係なので、板を常設しても機械は止まらない。
   *
   * 板はスロープのローカル座標で置き、スロープの傾きにそのまま乗せる。
   * 閉 (angle 0) で流路に平行、開くと手前側が内へ寄って漏斗になる。
   */
  flipper: {
    enabled: true,
    seconds: 8.0,          // 開いている時間
    x: 1.75,               // 板の中心 x (±)。閉じている間に絞りすぎないよう外寄り
    dz: 0.02,              // スロープのローカル z
    length: 1.70,          // 板の長さ
    thickness: 0.16,
    height: 0.42,
    dy: 0.50,              // スロープのローカル y。下端が坂の天面すれすれ
    // 開いたときの回転 (rad)。板は中心で回るので、角度を大きくすると
    // 手前側が内へ寄るのと同時に奥側が外へ開く = 漏斗の口が広く、出口が狭くなる。
    // 実測: x=1.55 だと閉じているだけで START が 34回/分、x=1.90 で 21.6回/分。
    // 閉じているときの間隔は広く保ったまま、開いたときだけ絞りたいので長い板にしてある
    angle: 0.90,
    speed: 2.4,            // rad/s
  },

  /** ホッパー: 当たり分をまとめて降らせる装置 */
  hopper: {
    // 残数が多いほど速く吐く。ジャックポットの数百枚を定速で出すと
    // 1分以上ちょろちょろ落ち続けることになり、当たった実感が薄れる
    rate: 14,              // 枚/秒 (基本)
    accel: 0.18,           // 残1枚あたりの加算
    maxRate: 50,           // 枚/秒 (上限)
    y: 9.5,
    // 落とす位置は上段の「前寄り」。ここが一番シビアだった (実測):
    //   下段に直接ぶちまける → 前だけ詰まって押しても上に積み上がるだけ。払い出し停止
    //   上段の最奥に落とす   → 上段に溜まる一方で下段が痩せ、山が縁に届かない。払い出し停止
    //   上段の前寄り         → 上段と下段の両方に行き渡る。○
    // 上段デッキを奥へ 1.0 下げたので、ここも同じだけ下げてある (1.4 → 0.4)。
    // 据え置くとプッシャーの前面より手前に落ちて「下段に直接ぶちまける」になる
    z: 0.4,
    spreadX: 5.0,
    spreadZ: 0.35,
    // 場内が詰まると流れが止まるので、良く流れる範囲に収まるまで待つ。
    // 上限(585)いっぱいまで詰めると1ステップが20ms超になり60fpsを維持できない。
    // 実測: 200枚→5.1ms / 300枚→10.2ms / 400枚→16.2ms / 500枚→24.4ms
    fieldLimit: 330,
    stallRelease: 3.0,     // 秒。これだけ出せずにいたら上限を緩める
    // 【実測】緩めるときも「上限を無視」にしてはいけない。
    // ジャックポットの残り 340枚を吐かせたら場内が 581枚 (プールの上限 585) まで埋まり、
    // 1ステップが 28ms 相当まで伸びた。超過はこの枚数までに抑える
    stallOverflow: 60,
  },

  /**
   * アンチジャム。
   * 粒状体は「アーチ」を作って自力では崩れなくなることがあり、
   * 場内が満杯に近いと押しても1枚も落ちない状態で固まる (実測で再現)。
   * 一定時間まったく場外に出なくなったら、前縁付近にごく弱い前向きの力を加えて崩す。
   * 実機の筐体が微妙に振動しているのに相当する処理で、これが無いと稀に詰んだままになる。
   */
  antiJam: {
    enabled: true,
    seconds: 6.0,          // これだけ払い出しが止まったら介入する
    minMedals: 250,        // 場内がこれ以上のときだけ (少ないのは単に山が薄いだけ)
    impulse: 0.35,         // メダル1枚あたり。質量約0.63なので約0.55 unit/s の初速
  },

  audio: {
    masterVolume: 0.55,
    maxVoicesPerFrame: 5,  // 1フレームに鳴らす衝突音の上限
    minInterval: 0.012,    // 秒。これ未満の間隔では鳴らさない
    // しきい値。低いと山の中の微小な接触まで拾ってしまい、
    // 音が鳴りっぱなしになるうえイベント処理だけで 1ステップ 3ms 増える (実測)
    impactThreshold: 3200,
    impactFullScale: 28000,// この接触力で音量最大
    // 抽選ボールの落球音 (src/audio/ballFall/*.mp3)。
    // 素材ごとのばらつきは読み込み時にピーク 0.45 へ揃えてあるので、
    // ここは「合成の効果音に対してどれくらいの大きさで混ぜるか」だけ (audio/Sound.js)
    ballFallVolume: 0.55,
  },

  /**
   * プレイヤーの持ち枚数。
   * 投入で減り、払い出しで増える。0 になってしばらく何も落ちてこなければゲームオーバー。
   */
  wallet: {
    start: 300,
    // 壊れた/改竄されたセーブを弾くための上限。**ゲームのルールではない**。
    // 正常なプレイでここに届くことは無い数字にしておく
    sanityMax: 9999999,
    // 0枚になっても、場に残っているメダルがまだ落ちてくる可能性がある。
    // すぐ終了にすると理不尽なので、この秒数まったく増えなければ終了とする
    gameOverDelay: 8.0,
  },

  /** セーブデータ (DESIGN.md §11) */
  save: {
    key: 'medalweb:v1:save',
    version: 1,
    debounceMs: 2000,      // 値が変わってからまとめて書くまでの待ち
    // 初期配置の既定枚数。セーブがあればその fieldStock を使う。
    // 下段を奥へ 1.0 広げた (面積 +24%) ぶん、下段に敷く枚数を同じ密度で増やしてある。
    // 上段は形が変わっていないので据え置き: 168 : 63 → 168 : 78
    defaultFieldStock: 246,
    // 上段と下段の配分。246枚のときの 168 : 78 に合わせてある
    deckRatio: 0.68,
  },

  /**
   * 発射式の投入 (DESIGN.md §7.2)
   *
   * 上から落とすのではなく、**手前の発射口から上段デッキへ撃ち上げる**。
   * 狙いは自動で左右に首を振り、プレイヤーが決めるのは「撃つ瞬間」だけ。
   *
   * ■ なぜ首振りにしたか
   *   マウスで投入 x を自由に選べると、狙いは「置く」作業になって
   *   上手い下手が出ない。首振り + 発射にすると、同じ「x を選ぶ」でも
   *   タイミングを合わせる操作になる。長押しの連射も残るので、
   *   雑に撃つ / 狙って1枚ずつ撃つ、をプレイヤーが選べる。
   *
   * ■ 発射口を置ける場所は限られている
   *   手前の前縁 (z=6.0) はメダルが落ちていく道で、ここに物を置くと
   *   払い出しが止まる (DESIGN.md §7.7 / DESIGN_GIMMICKS.md §3.1 の実測)。
   *   ガラスは z 6.25〜6.55 の y≧1.5 にあるので、その内側の下、
   *   前縁からこぼれる流れより少し上、という隙間に置いてある。
   *
   * ■ 速度は「着地点」から逆算する
   *   speed を直に持たず、仰角と着地点 (landZ) から初速を数値解で求める。
   *   メダルには線形減衰が掛かっていて放物線の式では合わないので、
   *   物理と同じ積分で解いている (game/Launcher.js)。
   *   着地点の既定値 z=-0.6 は、上から落としていた頃の投入位置と同じ。
   *   **ここを変えると場内の平衡枚数と払い戻しが動く**ので、変えたら測ること。
   */
  launcher: {
    // 発射口の首の位置
    x: 0, y: 1.35, z: 6.15,
    elevation: 1.00,       // 仰角 (rad)。約57度
    // 首振りの片振幅 (rad) と往復周期 (秒)。
    // 振幅を大きくすると端は「浅く・外側」に着地する (射程は一定で扇形に散るため)。
    //
    // ここは landZ とセットで決まる。**着地の左右幅は range x sin(yawRange)** なので、
    // 下段を広げて landZ を奥へ 1.0 送った (= range 7.75 → 8.75) ぶん、
    // 角度を狭めないと端の着地が x=±5.6 まで外へ出て、上段の側壁 (内面 ±5.875) に
    // ぶつかって跳ね返る。asin(5.0 / 8.75) = 0.608 で以前と同じ ±5.0 に戻している
    yawRange: 0.608,
    period: 2.4,
    // 狙う着地点。y は上段デッキの天面、z はその奥行き。ここが飛距離そのもの。
    // 着地したメダルは水平の勢いのぶんだけ奥へ 0.5 ほど滑るので、
    // 背面壁の内面 (z=-3.0) にちょうど届く手前を狙う。
    // **首を振ると射程は一定のまま扇形に散る**ので、端では x=±5.0 / z=-1.0 と
    // 手前寄りになる。中央で撃つと最奥、端で撃つと手前の縁、という配分。
    // 背面壁を奥へ 1.0 下げたので、壁との相対を保つようここも -1.0 している
    landZ: -2.6,
    // 発射するメダルの姿勢は上から落としていた頃と同じ「少し傾けて回す」ままにする。
    // 水平に寝かせて撃つと山の積み方が変わり、実測で場内が増えて払い戻しが
    // 58% まで落ちた (投入の仕方だけを変えたつもりが、山の詰まり方まで変わる)
    barrel: 0.55,          // 砲身の長さ。メダルはこの先から出る
    recoil: 0.30,          // 発射の反動 (unit)。0.12秒で戻る
    // 着地予測の見た目
    guidePoints: 24,       // 軌道ガイドの分割数
    markerR: 0.62,         // 着地点のリングの半径
  },

  input: {
    dropCooldown: 0.16,    // 秒。連打で同じ場所に重なって生成されるのを防ぐ
  },

  render: {
    shadows: true,
    cameraPos: { x: 0, y: 11.0, z: 22.0 },
    cameraTarget: { x: 0, y: 1.0, z: 0.5 },
    fov: 38,

    /*
     * 画面が狭いときの画角合わせ (core/Renderer.js の _fitCamera)。
     *
     * 縦持ちのスマホは aspect が 0.46 ほどになる。fov を固定したままだと
     * 横に見えるのは 7 unit しかなく、幅 15.4 unit の筐体が完全にはみ出す。
     * そこで「最低でもこの幅と高さは映す」という箱を決めて、
     * 収まるところまで画角を広げる (widescreen では何も起きない)。
     *
     * 広げるだけだと縦持ちで 100 度を超えて歪むので、maxFov で頭を打ったら
     * 残りはカメラを引いて稼ぐ。引いても d * tan(fov/2) は変わらないので
     * 映る範囲は同じ。
     */
    fitWidth: 17,          // 筐体の幅 15.4 + 余白
    fitHeight: 15,         // 筐体の高さ (受け皿 〜 上枠) + 余白
    maxFov: 62,
    // 縦持ちでは筐体が画面の中ほどに来るよう、注視点を上げる。
    // 空いた上下が HUD の置き場になる
    cameraTargetPortrait: { x: 0, y: 3.4, z: 0.5 },
  },
};

/**
 * 本番ビルドでは CFG を凍結する。
 *
 * 凍結しないと `CFG.pusher.strokeHalf = 2` の一行で払い出しが蛇口になる。
 * 実測で書き換え可能なことを確認済み。
 *
 * 開発ビルドでは凍結しない。デバッグパネル (lil-gui) が CFG を直接書き換えて
 * パラメータを詰める作りになっているため (DESIGN.md §5)。
 * 本番コードで CFG に代入している箇所は無いので、凍結しても壊れない
 * (Debug.js 以外に代入が無いことを grep で確認済み)。
 *
 * これも「防御」ではない。バンドルを書き換えれば無効化できる。
 * コンソールから1行、という手軽さを潰すだけのもの。
 */
function deepFreeze(o) {
  for (const k of Object.getOwnPropertyNames(o)) {
    const v = o[k];
    if (v && typeof v === 'object' && !Object.isFrozen(v)) deepFreeze(v);
  }
  return Object.freeze(o);
}

if (!import.meta.env.DEV) deepFreeze(CFG);
