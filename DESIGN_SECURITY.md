# チート対策・セキュリティ設計

作成: 2026-09-04 / 対象: 本リポジトリ

---

## 0. 前提 —— このゲームはチートを防げない

サーバが無く、物理も抽選も持ち枚数も全部ブラウザの中にあります。
**プレイヤーはプロセスの所有者なので、原理的にチートは防げません。**

したがってこの文書は「防御」の設計書ではありません。目的は3つだけです。

1. **出荷事故を無くす**（デバッグ機能を本番に混ぜない）
2. **カジュアルな改竄のコストを上げる**（コンソールから1行、を潰す）
3. **壊れた値をゲームの内部状態に入れない**（改竄対策であると同時にバグ耐性）

そして現状 **ランキングも課金も対人要素も無いので、チートの被害者は本人だけ**です。
重い対策に投資する価値はまだありません。ここで実装したのは、
安いわりに効くものと、明らかな出荷事故だけです。

---

## 1. 実測した攻撃面（対策前）

推測ではなく、動いているページで実際に試した結果です。

| # | 攻撃 | 実測結果 |
|---|---|---|
| A | `Math.random = () => 0.9999` | **5回中5回 blue7（＝ジャックポット当選）** |
| B | `game.wallet.medals = 1e9` | 通る |
| C | D キー → デバッグパネル | 本番バンドルに同梱。`dist` に「ジャックポットを撃つ」「強制する絵柄」あり、lil-gui 50箇所 |
| D | セーブ JSON を直接編集 | `restore({medals: 999999999})` が**素通り**。`earned: -5` も通る |
| E | `game.CFG.pusher.strokeHalf = 2` | 書き換え可能。払い出しが蛇口になる |
| F | セーブに `lostAccum: 1e9` | 次の `onLost()` の while ループが10億回まわり**ゲームが固まる** |

### A の仕組み（いちばん安いチートだった）

重み付き抽選が「合計1000の目盛から引く」形なので、1.0 に近い値を返させると
必ず最後の絵柄（青7 = JP当選）に落ちます。

```js
let r = Math.random() * 1000;          // 0.9999 * 1000 = 999.9
for (const s of symbols) { r -= s.weight; if (r <= 0) return s; }
// 640+165+100+62+18+8+4 = 997 まで引いても r > 0 → 最後の blue7 で確定
```

---

## 2. 実装した対策（対策しやすい順）

### 2.1 デバッグパネルを本番ビルドから外す ★出荷事故

`main.js` が `Debug` を無条件 import していたので、リリース版でも
D キーで「ジャックポットを撃つ」「この絵柄でまわす」「メダル50枚投入」が押せました。

動的 import を `import.meta.env.DEV` で囲い、本番では分岐ごと消えるようにしています。

```js
let debug = NOOP_DEBUG;
if (import.meta.env.DEV) {
  const { Debug } = await import('./ui/Debug.js');
  debug = new Debug({ ... });
}
```

**実測（本番ビルドの検証）:**

| | 対策前 | 対策後 |
|---|---|---|
| 「ジャックポットを撃つ」 | 1箇所 | **0 ✓** |
| 「強制する絵柄」 | 1箇所 | **0 ✓** |
| lil-gui | 50箇所 | **0 ✓** |
| D キーを押す | パネルが開く | **何も起きない ✓** |

副次効果としてバンドルが 3714KB → 3673KB（lil-gui と stats.js が落ちた）。

### 2.2 `window.game` を開発ビルドだけにする

`wallet` `jackpot` `slot` `CFG` `hopper` まで生で公開していました。
`import.meta.env.DEV` で囲っています。

モジュール内の実体は残るので**防御ではありません**が、
コンソールから名前で辿れなくなるだけで敷居はかなり上がります。

**実測:** 本番ビルドで `typeof window.game === 'undefined'` ✓

### 2.3 乱数の入口を1つにする（`src/core/Rng.js`）

`Math.random` に依存するのをやめ、種だけ `crypto.getRandomValues` から取る
自前の PRNG（sfc32）に差し替えました。状態はモジュール内に閉じているので、
`window.game` を落としてある本番ではコンソールから触れません。

`src/` の `Math.random()` 36箇所のうち、ゲームの挙動に関わる 34箇所を置き換え。
残り2つは意図的に据え置きです。

- `SaveStore.js` … `crypto.randomUUID` が無い環境での userId 生成。抽選ではない
- `Debug.js` … 本番に載らない

**実測:**

```
対策前: Math.random = () => 0.9999  →  [blue7, blue7, blue7, blue7, blue7]
対策後: Math.random = () => 0.9999  →  [bell, bell, blank, blank, blank, blank, blank, replay]
```

**これも完全ではありません。** バンドルを書き換えられたら終わりですし、
devtools でヒープを覗く手もあります。潰したのは「コンソールから1行」という手軽さだけです。

### 2.4 セーブの値を範囲に押し込む（`Wallet` / `SpecialMedals` / `main.js`）

`localStorage` の中身はプレイヤーが自由に書き換えられます。
改竄そのものは防げませんが、**壊れた値をゲームの内部状態に入れないこと**はできます。

方針は **「捨てずに直す」**。厳しく弾くと、こちらのバグや仕様変更で
正直なプレイヤーの記録まで消してしまいます。

**実測（対策後）:**

| 入力 | 結果 |
|---|---|
| `medals: 999999999` | 9999999 にクランプ |
| `best: -1` | 持ち枚数まで引き上げ（矛盾を修復）|
| `earned: -5` | 0 |
| `inserted: 1e30` | 9999999 |
| `games: NaN` | 0 |
| `slot.wins: 999` (spins は 3) | 3 に切り下げ |
| `byId: { __proto__: 9, evil: 100 }` | 未知キーを破棄。`bell` だけ残る |
| `jp.paid: "999"` (文字列) | 0 |
| **`lostAccum: 1e9`** | **0。`onLast()` が 0.3ms で完了（固まらない）** |
| `stock.gold: 99` | 3（上限）にクランプ |

`Jackpot.restore` と `fieldStock` は元からクランプ済みだったので、
やったのは**それに揃えた**だけです。`fever.steps` も範囲に入れました。

`lostAccum` はチートというより**セーブ経由でフリーズさせられる穴**でした。

### 2.5 本番ビルドで `CFG` を凍結する

`CFG.pusher.strokeHalf = 2` の一行で払い出しが蛇口になっていました。
`deepFreeze(CFG)` を本番だけかけています。

開発ビルドでは凍結しません。デバッグパネルが CFG を直接書き換えて
パラメータを詰める作りだからです（`DESIGN.md §5`）。
`Debug.js` 以外に CFG への代入が無いことを grep で確認済みなので、凍結しても壊れません。

**実測:** 本番ビルドが例外なく起動し、HUD も物理も正常 ✓

---

## 3. あえてやっていないこと

| 案 | やらない理由 |
|---|---|
| **セーブへの署名（HMAC）** | 鍵がバンドルに同梱される以上、少し読める人には署名し直される。「鍵と一緒に配る署名は署名ではない」。止まるのは devtools で JSON を直接書き換える層だけで、§2.4 を先にやったほうが得 |
| **devtools 検出・`debugger` ループ** | 迂回は一瞬なのに、正規のデバッグとユーザー体験だけを壊す |
| **難読化を「対策」と数えること** | 既に minify 済みで、読む気のある人は止まらない |
| **持ち枚数を WASM 側へ持っていく** | 難度は上がるが、ヒープを直接編集されれば同じ。大工事に見合わない |
| **厳密な整合性チェックでセーブを破棄** | 例: `earned <= inserted + 払い出し` は、ホッパー経由の戻りと初期在庫があるため正確に書けない。**誤検知で正直なプレイヤーの記録を消すほうが害が大きい** |

---

## 4. 現状のセキュリティ（チート以外）

| 項目 | 状態 |
|---|---|
| XSS | `innerHTML` / `dangerouslySetInnerHTML` **不使用**。ユーザー入力の表示箇所なし → リスクほぼ無し |
| 依存の脆弱性 | `npm audit --omit=dev` → **0件** |
| ソースマップ | 本番では出力されない（Vite の既定）✓ |
| localStorage | オリジン単位。他サイトからは読めない ✓ |
| CSP | 未設定。外部スクリプトを一切読んでいないので優先度は低い。付けるなら WASM のため `wasm-unsafe-eval` が要る |

---

## 5. サーバを足すときに必ず読むこと

ここまでの対策を全部やっても、**クライアントが申告した数字を信じてはいけない**
という事実は1ミリも変わりません。ランキングを作るなら選択肢は3つです。

1. **入力ログを送ってサーバで再現** — Rapier は決定的に回せるが、
   浮動小数の環境差で結果がずれるリスクが高く、重い
2. **統計的な外れ値検出** — 「1分あたりの獲得枚数が理論上不可能」をサーバ側で弾く。現実的
3. **検証しないと明示する** — 「自己申告」と書いてしまう

そして `DESIGN.md §11.3` が既に書いていることを繰り返します。

> これはブラウザを識別するもので、人を識別するものではありません。
> **この ID を認証の代わりにしてはいけません。**

`userId` は将来サーバを足すときの最大の地雷です。
