# メダルゲーム（コインプッシャー）設計書

作成: 2026-09-03 / 最終更新: 実装完了時に実測値へ改訂 / 対象: ブラウザ（WebGL2）

> この文書は実装前の設計に、実装で判明した事実（特に §2.4 と §5）を反映させた版です。
> 実装で覆った箇所には「実測」と明記しています。
>
> **ギミック（チャッカー／フィーバー／ジャックポット／3段クルーンなど）は
> [DESIGN_GIMMICKS.md](DESIGN_GIMMICKS.md) に、
> チート対策とセキュリティは [DESIGN_SECURITY.md](DESIGN_SECURITY.md) に分けてあります。**
> 本書は「メダルが押し出されて落ちる」という土台の話に限ります。

---

## 0. 最初の問いへの回答

**「three.js のみで作れるか」→ 物理演算については No。ただしブラウザだけで完結するのは Yes。**

three.js は**描画専用**で剛体力学を持っていません。コインプッシャーの本質は
**数百枚の薄い円盤が積み重なり、押し合い、崩れる**ことなので、接触点を大量に解く剛体ソルバが必須です。

構成は **three.js（描画） + Rapier（物理）** の2本立て。どちらもブラウザ内完結、サーバ計算は不要です。

---

## 1. 技術スタック

| 役割 | 採用 | バージョン | 備考 |
|---|---|---|---|
| 描画 | three.js | 0.185.x | WebGLRenderer |
| 物理 | @dimforge/rapier3d-compat | 0.20.x | Rust製WASM。`-compat` は WASM を base64 同梱するので Vite の追加設定が不要 |
| UI | React | 19.x | HUD と演出。`@vitejs/plugin-react` |
| UI の型 | TypeScript | 7.x | **UI だけ**。ゲーム本体は JS のまま（`allowJs` + `checkJs: false`）|
| ビルド | Vite | 8.x | dev server + HMR |
| デバッグUI | lil-gui / stats.js | 0.21 / 0.17 | 物理パラメータの実機調整に必須 |

`await RAPIER.init()` が必要な非同期初期化のため、起動フローの先頭で必ず待ちます。

---

## 2. ゲーム機構の物理モデル化

### 2.1 断面（側面図・実装後）

```
   奥(-Z) ─────────────────────────────────── 手前(+Z)
   ┌────┐
   │背面│  ← プッシャーの後端はこの中に隠れている
   │壁  │   ▓▓▓▓▓▓▓▓▓  上段の山
   │    │ ┌──────────────┐
   │    │ │  プッシャー   │ ⇄ ストローク 1.9 unit
   │    │ └──────────────┘
   └────┘   下段テーブル ▓▓▓▓▓▓▓      ⟵ ⟵ ⟵ [発射口]
   ═════════════════════════════════╡      メダルは手前から
                          落下口 ↓          撃ち上げて上段に乗せる
                        [ペイアウト]
```

### 2.2 二段構造がゲーム性の核

- **上段（プッシャーの上面）**: 前進で山ごと運ばれ、前縁から下段へこぼれる
- **下段（固定テーブル）**: プッシャーの**前面**が下段の山を押し、落下口から落ちる

### 2.3 剛体構成表

| オブジェクト | 剛体タイプ | コライダー | 理由 |
|---|---|---|---|
| プッシャー | **KinematicPositionBased** | Cuboid | 動的にすると山に押し返されて止まる |
| 下段テーブル | Fixed | Cuboid ×2 | 手前を細くして左右にサイドポケットを作る |
| 背面壁 | Fixed | Cuboid（厚いブロック） | プッシャー後端を隠す。**輸送の要**（§2.4） |
| 左右の壁 | Fixed | Cuboid ×2 | |
| ガラス | Fixed | Cuboid | 下端 y=1.5 より下は開口＝払い出し口 |
| 天井（不可視） | Fixed | Cuboid | 弾かれたメダルの脱走防止 |
| メダル | **Dynamic** | Cylinder | CCD + soft-CCD 有効 |
| 抽選ボール | **Dynamic** | Ball | 常に3個。落ちるとスロットが回る（§7.4）|

Kinematic は「無限質量」。プッシャーは何があっても止まりません。これが正しい挙動です。

### 2.4 【最重要・実測で判明】上段の輸送機構

**設計時に見落としていた点。ここを外すとゲームが一切成立しません。**

プッシャー上面に乗ったメダルは、摩擦で**前進でも後退でもプッシャーと一緒に動く**。
つまり放っておくと往復するだけで前に進みません。実際、初期実装では
90秒回してもメダルが1枚も落ちませんでした（上段に264枚溜まったまま）。

前に送るには「後退時にメダルを堰き止める固定物」が必要です。

```
  後退 → 背面壁に押し当てられた山はその場に残る（プッシャーに対しては相対的に前進）
  前進 → 山はプッシャーと一緒に前へ運ばれる
  → 1周期ごとに山全体が前進し、前縁からこぼれ落ちる
```

この「ラチェット」が働く条件は2つあります。

1. **プッシャーが背面壁の内側まで潜り込んでいること。**
   前進端でも後端が壁の内面より奥にあるようにする。上段に固定棚を置く案も試したが、
   棚は動かないぶんメダルが溜まる一方の死に領域になり、輸送も止まった（実測）。
2. **山が壁から前縁まで途切れず繋がっていること。**
   隙間があるとそこで力の連鎖が切れ、前方の山は動かない。
   → **上段デッキを浅くする**ことで、少ない枚数でも密に埋まるようにした。
   デッキを 10 unit から 2.25〜4.15 unit へ縮めたのが決定打（実測: 払い出し 0 → 成立）。
   下段を広げたときもこの奥行きは動かしていません（§7.10）。

---

## 3. 単位系とスケール

実寸をそのまま使うと破綻します（メダル厚 1.8mm = 0.0018 は物理エンジンの許容誤差以下）。

**1 unit = 25 mm（メダル直径）** と定義。時間は実時間のまま。
重力を単位に合わせて換算すれば、挙動は実寸と物理的に同一です（次元解析上の相似）。

| 量 | 実寸 | ゲーム内 |
|---|---|---|
| メダル半径 | 12.5 mm | `0.5` |
| メダル厚さ | 1.8 mm | `0.10`（安定性のため実機より厚め。見た目には分からない） |
| フィールド幅 | 350 mm | `14.0` |
| プッシャー全ストローク | 47 mm | `1.9` |
| 上下段の段差 | 30 mm | `1.2` |
| 重力 | 9.81 m/s² | `392.4` unit/s²（= 9.81 / 0.025） |

---

## 4. 座標系とレイアウト（実装値）

- **X**: 左右（右が +） / **Y**: 上（重力は -Y） / **Z**: 奥行き（**手前が +Z**）
- 下段テーブルの上面が `y = 0`、上段（プッシャー上面）が `y = 1.2`

| 要素 | 中心 (x, y, z) | サイズ (w, h, d) | 範囲 |
|---|---|---|---|
| 下段テーブル（主） | (0, -0.5, -1.25) | 14.0 × 1.0 × 8.5 | z: -5.5 .. 3.0 |
| 下段テーブル（手前）| (0, -0.41, 4.5) | 11.0 × 1.0 × 3.0 | z: 3.0 .. 6.0、**X軸まわりに -0.06 rad 傾斜**（§7.6）|
| プッシャー | (0, 0.6, -2.3 ± 0.95) | 14.4 × 1.2 × 5.0 | 前面 z: -0.75 .. 1.15 |
| 上段の側壁 | プッシャー上面の左右 (x = ±6.0) | 各 0.25 × 0.30 × 5.0 | 手前は塞がない（§7.7）|
| 背面壁 | (0, 6.0, -4.25) | 14.0 × 12.0 × 2.5 | 内面 z = -3.0 |
| 左右の壁 | (±7.25, 6.0, 0.5) | 0.5 × 12.0 × 12.0 | 内面 x = ±7.0 |
| ガラス | (0, 6.75, 6.4) | 14.0 × 10.5 × 0.3 | y: 1.5 .. 12 |
| 天井（不可視） | (0, 16.0, 0.5) | 15.0 × 0.5 × 12.0 | |
| 発射口 | (0, 1.35, 6.15) | — | 首振り ±0.608 rad / 仰角 1.00 rad（§7.2）|

- **サイドポケット**: 手前テーブルの左右に開いた `x: ±5.5〜7.0, z: 3.0〜6.0` の隙間。落ちるとロスト
- **落下口**: テーブル前縁 `z = 6.0` の先。ガラス下端 `y = 1.5` より下が開口
- 前進端のプッシャー前面（z=1.15）から落下口（z=6.0）まで **4.85 unit = メダル約5枚分**。
  ここを深くしすぎると山が縁まで届かず、いつまでも払い出されません（§7.10 に実測）。

カメラ: `PerspectiveCamera(38°)` を `(0, 11, 22)` に置き `(0, 1, 0.5)` を注視。
視点操作は右ドラッグ（左クリックは発射に使うため）。

---

## 5. パラメータ（実装値・実測で調整済み）

すべて `src/config.js` に集約し、lil-gui（Dキー）から実行中に調整できます。

```js
physics: { gravity: -392.4, timestep: 1/60, maxSubSteps: 3, solverIterations: 8, ccd: true }
medal:   { radius: 0.5, thickness: 0.10, density: 8.0,
           friction: 0.26, restitution: 0.02,
           linearDamping: 0.25, angularDamping: 0.45, maxCount: 300 }
pusher:  { strokeHalf: 0.95, period: 1.9, friction: 0.35 }
table:   { friction: 0.32, restitution: 0.0 }
```

### 実測: 払い出し量を決めるのは「ストローク」

150秒間、0.4秒に1枚（計360枚）投入し続けたときの獲得枚数。

| 片振幅 / 周期 | メダル摩擦 | 獲得 | ロスト | 判定 |
|---|---|---|---|---|
| 0.80 / 2.2 | 0.22 | 17 | 1 | ほとんど落ちない |
| 0.90 / 2.0 | 0.22 | 239 | 26 | 良好 |
| **0.95 / 1.9** | **0.26** | **249** | **63** | **採用**（払い戻し約69%） |
| 1.20 / 1.4 | 0.18 | 285+ | 4 | 出過ぎる |

摩擦の影響も小さくはないが、**ストロークの効きが圧倒的**でした。
払い出しを渋くしたいならまずストロークを縮めます。

---

## 6. アーキテクチャ

### 6.1 ファイル構成

```
medal_web/
├── index.html            HUD の DOM と CSS
├── package.json / vite.config.js
├── DESIGN.md             本書
└── src/
    ├── main.js           起動シーケンス、ループ配線、初期配置
    ├── config.js         全パラメータ（上記）
    ├── core/
    │   ├── Renderer.js   scene / camera / light / OrbitControls / 環境マップ
    │   └── Loop.js       固定タイムステップ + 描画補間
    ├── physics/
    │   └── World.js      Rapier 初期化、EventQueue、Fixed ボックス生成ヘルパ
    ├── audio/
    │   └── Sound.js      WebAudio による効果音の合成（音声ファイル不使用）
    ├── save/
    │   └── SaveStore.js  localStorage への読み書き（§11）
    ├── game/
    │   ├── Cabinet.js    筐体の静的部品（メッシュ + コライダーを対で生成）
    │   ├── Pusher.js     キネマティック往復
    │   ├── LotteryBall.js 抽選ボール（フィールドを転がる球）
    │   ├── MedalPool.js  InstancedMesh + 剛体プール
    │   ├── Launcher.js   発射式の投入（首振り・弾道の逆算・着地ガイド）
    │   ├── Hopper.js     当たり分をまとめて降らせる装置
    │   ├── SlotMachine.js スロットの抽選テーブルと保留
    │   ├── SlotDisplay.js 筐体前面の液晶パネル（演出の描画と進行）
    │   ├── Wallet.js     持ち枚数・通算成績・ゲームオーバー判定
    │   ├── AntiJam.js    山がアーチを組んで固まったときの解除
    │   └── Payout.js     落下判定 → クレジット / ロスト
    └── ui/                      React 製の UI レイヤ（§6.5）
        ├── mount.tsx             React ルートの生成、ゲーム側へ渡すハンドル
        ├── App.tsx               UI の組み立て
        ├── store.ts              useSyncExternalStore 用の外部ストア
        ├── symbols.ts            絵柄の定義に型を付ける（配当表用）
        ├── theme.css             共有トークンと .panel
        ├── Debug.js              lil-gui / stats / Rapier デバッグ描画（Dキー）
        └── components/
            ├── WalletPanel.tsx     持ち枚数・通算成績・獲得ポップ
            ├── PayTablePanel.tsx   配当表・保留ランプ
            ├── StatsPanel.tsx      FPS / 枚数 / ステップ時間
            ├── ControlsBar.tsx     操作ヘルプ・消音表示
            ├── GameOverOverlay.tsx ゲームオーバー表示とリスタート
            ├── LoadingOverlay.tsx  WASM 読み込み待ち・起動失敗表示
            └── *.module.css        各コンポーネントのスタイル（CSS Modules）
```

### 6.2 メインループ（固定タイムステップ + 補間）

物理は**必ず固定ステップ**。可変 dt は積層シーンで即座に破綻します。

```
accumulator += min(realDelta, 0.25)      // スパイク時の暴走を防ぐクランプ
while (accumulator >= 1/60 && steps < 3) {
    dispenser.update(dt)
    pusher.update(t + dt)                // 次ステップ終了時の目標位置
    world.step()
    pool.captureTransforms()             // prev ← curr, curr ← 剛体の最新値
    payout.update()
    accumulator -= 1/60; steps++
}
alpha = accumulator / (1/60)
pool.sync(alpha)                         // prev と curr を lerp / slerp
```

`steps < 3` は「重いフレーム → 物理が遅れる → さらに重くなる」死のスパイラルを断つため。

### 6.3 描画

300枚を `Mesh` 300個にするとドローコール300回。**`InstancedMesh` 1個 = 1ドローコール**にします。
実測のドローコール総数は 50 前後（うちメダルは1）。

### 6.4 メダルプール

剛体の生成/破棄はコストが高く GC も誘発するため、実行中は作らない・壊さない。
起動時に上限数の剛体を作り、未使用のものはコライダーを無効化して地中に沈めておきます。

**満杯時は投入を断る**（既存のメダルを消さない）。山から抜くと痩せて
「押しても落ちない」状態になるためです。

---

### 6.5 React UI とゲームループの接続

UI は React（TypeScript）、ゲーム本体は素の JavaScript のままです。
`src/ui/mount.tsx` が両者の唯一の境界で、**ゲームループは React を一切知りません**。

```
ゲームループ (60Hz)
   └─ ui.setGame({credit, lost, hold, muted, ...})   ← 毎フレーム呼んでよい
   └─ ui.setStats({fps, medals, stepMs})             ← 4Hz に間引いて呼ぶ
   └─ ui.slot.play(result)                           ← Promise。Jackpot が await
          ↓ ストアに書く
     Store (src/ui/store.ts)
          ↓ 変化したときだけ通知
     useSyncExternalStore → 購読しているコンポーネントだけ再レンダリング
```

#### 毎フレーム setState しないための工夫

素直に `setState` を毎フレーム呼ぶと React が 60fps で再レンダリングし、描画予算を食います。
対策は2段構えです。

1. **ストア側で浅い比較をする。**
   `Store.set()` は渡されたキーを既存値と `Object.is` で比較し、
   ひとつも変わっていなければスナップショットの参照を差し替えず、通知もしません。
   `useSyncExternalStore` は参照が同じなら再レンダリングしないので、
   クレジットが動かない間は毎フレーム呼んでもコストはループ1回ぶんです。
2. **毎フレーム必ず変わる値は呼び出し側で間引く。**
   FPS・メダル枚数・ステップ時間は比較が効かないので、
   `main.js` の `STATS_INTERVAL`（0.25秒）で 4Hz に落としてから渡します。
   これらは `statsStore` に分けてあるので、更新してもクレジット表示は再レンダリングされません。

同じ理由で、スロットのリールが回っている間の絵柄の差し替えは
`SlotPanel.tsx` のコンポーネント内部（`useState` + `setInterval`）で完結させています。
ストアに書くと 18Hz で UI 全体が再レンダリングされてしまうためです。

#### スロットの演出は React ではなく 3D 側にある

当初はスロットの演出も React のオーバーレイでしたが、**画面中央に被さって盤面が見えなくなる**ため、
筐体前面の液晶パネルとして 3D シーンの中に移しました（§7.9）。
React が担当するのは HUD（持ち枚数・配当表・計測値・ヘルプ・ゲームオーバー）だけです。

#### DOM 版から持ち越した教訓

- **透明度をアニメーションさせない。**
  物理でメインスレッドが詰まっている環境では、CSS の `transition` も keyframes も
  途中の値で止まったまま張り付くことがあり、パネルが半透明のままになります（§9）。
  最初 `transition` で踏み、keyframes に替えたあとゲームオーバー画面でもう一度踏みました。
  出し入れは React の条件付きレンダリングで行い、**登場の動きは transform だけ**で付けています。
  transform なら途中で止まってもカードが少しずれるだけで済みます。
- **アニメーションの打ち直しは `key` で行う。**
  獲得ポップは `key={gainSeq}` で要素ごと作り直します。
  DOM 版でやっていた「クラスを外す → `offsetWidth` を読んでリフローを強制 → 付け直す」が不要になりました。

#### TypeScript の範囲

`tsconfig.json` は `allowJs: true` / `checkJs: false`。
UI（`.ts` / `.tsx`）だけが型チェックの対象で、ゲーム本体の JS は import できるだけです。
`npm run typecheck` で確認できます（Vite のビルドは型チェックをしないため、別途走らせる必要があります）。

---

## 7. 主要ロジック

### 7.1 プッシャーの駆動

```js
const z = baseZ + strokeHalf * Math.sin(2 * Math.PI * t / period);
body.setNextKinematicTranslation({ x, y, z });
```

`setTranslation()` ではなく **`setNextKinematicTranslation()`**。
前者は瞬間移動になり接触が正しく解かれず、メダルが吹き飛びます。
駆動波形は正弦波（三角波より端の加速度が滑らかで、メダルを弾きにくい）。

### 7.2 投入 — 手前から撃ち上げる

**メダルは上から落ちてきません。手前の発射口から上段デッキへ撃ち上げます。**

- 狙い（首の角度）は**自動で左右に振れ続ける**。プレイヤーが決めるのは撃つ瞬間だけ
- クリック / Space で1枚、長押しで連続（クールダウン 0.16秒）
- **クリックが2つの物理ステップの間に収まると取りこぼす**ため、押下をラッチする
- 首振りは三角波（等速）。正弦波にすると端で遅くなり、端を狙うのだけが簡単になる

#### なぜ「置く」から「撃つ」に変えたか

マウスで投入 x を自由に選べる形だと、狙いは**置く作業**で、上手い下手がほとんど出ません。
首振り + 発射にすると、同じ「x を選ぶ」でもタイミングを合わせる操作になります。
長押しの連射は残してあるので、雑に撒く / 狙って1枚ずつ撃つ、をプレイヤーが選べます。

#### 発射口を置ける場所は1か所しかない

手前の前縁（z=6.0）はメダルが落ちていく道で、ここに物を置くと払い出しが止まります
（§7.7 / DESIGN_GIMMICKS.md §3.1 の実測）。ガラスは z 6.25〜6.55 の y≧1.5 なので、
**その内側の下、前縁からこぼれる流れより少し上**という隙間に置いてあります
（`(0, 1.35, 6.15)`、コライダーは持たせない）。

#### 初速は「着地点」から逆算する

`CFG.launcher` が持つのは仰角（57°）と着地点（デッキ天面の z=-2.6）で、初速は起動時に
数値解で求めます。放物線の公式では合いません:

- メダルには線形減衰 (0.25) が掛かる
- **Rapier は位置を「更新前後の速度の平均」で進める**。素直なオイラー法で解くと
  1回の飛翔で 0.4 unit ぶん短く見積もる（実測）
- 飛翔は 0.25秒 = 物理15ステップ程度しかないので、着地の判定を刻んだままにすると
  飛距離が 0.6 unit 単位の階段になり、二分探索が段差に落ちる（着地は補間して求める）

物理と同じ刻み・同じ順序で積分して二分探索した結果、着地点の予測は実測と **±0.1 unit**
で一致します。着地点のリングと軌道のガイドは、この同じ積分から引いています。
**狙って撃つ台で着地点が分からないのは、タイミングを狙わせないのと同じ**なので、
ガイドは常時表示（山に埋もれないよう深度テストを切って手前に描く）。

#### 【実測】「投入の仕方」を変えると山の積み方まで変わる

発射に変えるとき、メダルを**水平に寝かせたまま**撃ち出す実装にしていました。
見た目はコインらしいのですが、経済が変わりました（ホッパー経由の払い出しを止め、
0.55秒に1枚・180秒の条件で比較）:

| 投入の仕方 | 払い戻し | 場内の平衡 |
|---|---|---|
| 上から落とす（従来）| 68.3% | 213〜222 |
| 発射・**水平に寝かせて** | 58.5% | 185→216（増え続ける）|
| 発射・**従来と同じ姿勢**（少し傾けて回す）| 65.0% | 218〜253 |

水平に揃った円盤はきれいに積み重なるぶん山が締まり、同じ体積により多く溜まります。
**投入の姿勢は山の詰まり方に効く**ので、姿勢は従来のまま（`spawn` の既定の傾き＋回転）に
戻しました。この状態なら払い戻しも平衡枚数も従来と同じ範囲です。

#### 飛距離は「デッキの最奥に届くか」で決めている

狙いの着地点 `landZ = -2.6` は、**上段デッキのいちばん奥（背面壁の内面 z=-3.0）に
届く**ところに置いてあります。着地したメダルは水平方向の勢いで奥へ 0.5 unit ほど
滑るので、実際には壁ぎわで止まります。初速は 60.3 unit/s
（デッキを奥へ下げる前・`landZ = -1.6` のときは 56.4、`landZ = 0.0` だった頃は 49.5）。

首を振っても**射程は一定**なので、着地点は扇形に散ります。中央で撃つと最奥（z=-2.6）、
端で撃つと `x = ±5.0, z = -1.0` と手前寄りになる、という配分です。
**射程が伸びたら首振りの角度を絞ること**。着地の左右幅は 射程 × sin(yawRange) なので、
角度を据え置くと扇が外へ広がって上段の側壁に当たります（§7.10）。

飛距離を伸ばすと山の奥が厚くなるぶん戻りが遅くなりますが、実測では
**払い戻しへの影響はほぼありません**（上と同じ条件・135秒。
払い出された枚数の割合 62.1%、場内 160〜200枚。`landZ = 0.0` のときは 65.0% / 218〜253枚）。

### 7.3 落下判定

センサーコライダーではなく**座標判定**を採用。
テーブル下面が y=-1 なので、y < -2 まで落ちたメダルは
「手前の払い出し口」か「左右のサイドポケット」を必ず通っており、どちらかは x で一意に決まります。

判定後もすぐには消さず、y < -14 まで落ちきってから回収します。
「落ちて消えた」ではなく「落ちていった」ように見せるためです。

---

## 7.4 抽選ボールとスロット（ゲーム性）

### 引き金は「フィールドを転がるボール」

フィールドに大きな球が**常に3個**転がっていて、メダルと同じようにプッシャーに押されます。

```
  上段デッキ    ●    ●    ●   ← メダルの山と一緒に押される
  ────────────────┐
  下段テーブル      ●
  ══════════════════╡
           落下口 ↓
     払い出し口に落ちる → スロットが回る
     サイドポケットに落ちる → ハズレ（抽選なし）
  1.6秒後、上段の中ほどに戻ってくる（左・中・右に振り分け）
```

ボールはメダルのプールとは別管理で、Payout の走査対象にも入れていません。
ボールが落ちてもクレジットは増えません。

### 戻ってきた球を「その場に落ち着かせる」

これが無いと壊れます。戻ってきた球がそのまま転がって即座に落ち、
スロットが1〜3秒間隔で連続して回ってしまいました。

`LotteryBall` は 'settling' → 'live' → 'returning' の3状態を持ち、
戻した直後の `settleSeconds`（2.2秒）は **settling** として

- 毎ステップ角速度を 0 にする（回転を与えたまま落とすと着地して即転がり出す）
- 横方向の速度に 0.5 を掛けて殺す（落下は妨げない）
- コライダーの摩擦を 0.95 / 反発を 0.05 に上げ、跳ねずに着地させる
- **この間は落下判定をしない**

としています。落ち着いたら通常の摩擦 0.10 / 反発 0.5 に戻します。
実測では「戻ってから2秒以内に落ちた」回数が 0〜3 回／300秒まで下がりました（対策前は頻発）。

### 落下頻度は「戻す位置」でほぼ決まる

300秒あたりの落下回数（ボール3個での実測）:

| 戻す位置 | 落下回数 | 備考 |
|---|---|---|
| z = -0.8（上段の奥）| 4 | 山に埋もれて出てこない。詰まり検出が29回も走った |
| z = 0.0 | 9 | |
| **z = 0.6（デッキの中ほど）**| **12〜19** | **採用。** 平均20〜30秒に1回 |
| z = 1.2（段差の手前）| 14〜17 | |

摩擦も効きます。0.16 のままだとメダルの山に食い込んで止まり、
1個構成のとき240秒で1回しか落ちませんでした。0.10 まで下げてあります。

それでも稀に山に挟まるので、10秒間ほとんど動かなかったら
自分に弱いインパルスを与えて復帰します（300秒あたり3個合計で20回前後）。

### スロット

絵柄は8種類、盤面は 3列 x 3段 で、5本のペイライン上に同じ絵柄が3つ並べばアタリです
（3x3 化とライン数の設計は DESIGN_GIMMICKS.md §3.10）。構成と出現比率は
[参考記事](https://qiita.com/hunyu/items/4547d1d9fb734d81d189) に合わせています。

> 以下の表は改修前の値です。現在の実装値は DESIGN_GIMMICKS.md §3.10 を見てください。

| 絵柄 | 重み（/1000）| 配当 | 元記事 |
|---|---|---|---|
| ブランク | 400 | 0 | 770 |
| リプレイ ⟳ | 326 | 3 | 125 / 3枚 |
| チェリー 🍒 | 130 | 4 | 50 / 4枚 |
| ベル 🔔 | 105 | 10 | 40 / 10枚 |
| スイカ 🍉 | 21 | 15 | 8 / 15枚 |
| バー BAR | 10 | 40 | 4 / **150枚** |
| 赤7 | 5 | 80 | 2 / **350枚** |
| 青7 | 3 | 120 | 1 / **711枚** |

元記事から2点だけ変えています。

1. **配当枚数を縮めた。** 元記事の 150 / 350 / 711 枚はこの筐体が捌ける量を超えます。
   場内の上限が195枚なので、711枚は放出だけで10分以上かかります（§7.6 の実測）。
2. **ブランクの比率を 77% → 40% に下げた。**
   元記事は数秒に1回まわす台ですが、こちらは「ボールが落ちたとき」しか回りません。
   30秒に1回しか回らない台で77%がハズレだと、ほとんど何も起きないまま終わります。
   当たり絵柄どうしの比率は元記事のままです。

抽選は「どの絵柄で止まるか」を先に決め、リールはそこに止めに行きます（目押しなし。元記事も同じ）。
ハズレのときは3つとも別の絵柄にし、45%の確率で最初の2つだけ揃えてリーチをかけます。

### 経済の目安

実測（5分間・0.55秒に1枚投入）で、ボールの落下は13回、スロット13回、アタリ10回、
スロットからの払い出しは39枚でした。同じ時間にプッシャーからは397枚出ています。

つまり **今の配当だとスロットの寄与は1割弱**。
ボーナス演出としては成立していますが、もっと効かせたい場合は
`CFG.slot.symbols[].pay` を上げるのが素直です。目安として、
1回の当たりを「体感できるご褒美」にするなら中位（ベル・スイカ）を20〜40枚まで上げると、
プッシャーの払い出し十数秒ぶんに相当します。

---

## 7.5 効果音

ほとんどを **WebAudio でその場で合成**しています。
衝突音は「帯域を絞ったノイズのアタック + 金属的な倍音の余韻」で、
衝突の強さで音量・帯域・減衰が連続的に変わります。

衝突の検出には Rapier の **`CONTACT_FORCE_EVENTS`** を使います。
`COLLISION_EVENTS` ではなくこちらを選ぶのが要点で、しきい値を超えた接触だけが飛んでくるため
「聞こえるべき衝突」だけを拾えます。

**しきい値の実測**（`audio.impactThreshold`）:

| しきい値 | イベント数/ステップ | 備考 |
|---|---|---|
| 1600 | 16.6 | 山の中の微小な接触まで拾う。イベント処理だけで 1ステップ +3ms |
| 3200 | 0.5 | **採用。** 実際の衝突だけが残る |
| 4500 | 0.1 | 静かすぎる |

`EventQueue` は毎ステップ必ず drain してください。溜めると際限なく増えます。

### 例外: 抽選ボールの落球音だけ音声ファイル

`src/audio/ballFall/*.mp3` を `import.meta.glob` で全部拾い、
**フィールドの抽選ボールが落ちるたび**に1つをランダムに鳴らします（現在14種・計 453KB）。
ここだけ合成をやめている理由は、鳴る回数です。ボールが落ちるのは 20秒に1回ほどで、
衝突音のように毎秒何十回も鳴るものは合成のほうが「密度」を作れますが、
数秒に1回の見せ場は1発の質がそのまま印象になります。

- **払い出し口でもサイドポケットでも同じ音。** 「落ちた」ことは同じで、
  当たり（STEP が進む＝クルーンが回る）かハズレかを示すのは
  後続の `step()` / `lose()` のほうです
- 鳴らすのは `main.js` の物理ステップ、`balls.update()` の直後の1箇所だけ。
  同じステップで2個落ちても1回しか鳴らしません（重ねても濁るだけ）
- **読み込みは最初のユーザー操作から。** `resume()` で AudioContext ができた直後に
  非同期で取りに行きます。球が落ちるのはメダルを撃って盤面が動き出したあとなので、
  初回描画を待たせてまで先に取る必要はありません。間に合わなければ黙って鳴らさないだけです
- **master 経由で鳴らす。** `M` / SOUND ボタンの消音がそのまま効きます
- **ピークを揃えてから鳴らす。** 素材のピークは 0.16〜0.59（3.7倍）ばらついていて、
  そのまま混ぜると「同じ出来事の音違い」ではなく「録り方がバラバラ」に聞こえます。
  読み込み時に各ファイルのピークを 0.45 に正規化（持ち上げは4倍で頭打ち）。
  RMS ではなくピークで揃えるのは、余韻の長い素材を持ち上げすぎるとノイズまで上がるためです。
  実測で、鳴らしたときの master 出力ピークは 12回引いて 0.108〜0.126 に収まり、
  隣で鳴る合成音（`step()` 0.129 / `lose()` 0.075）と同じ幅に入りました。
  ピークは**全チャンネルの最大**を見ること。片チャンネルだけで測ると、
  左右で音量の違う素材の大きいほうがそのまま出てきます（実測でそこだけ 0.171 に跳ねた）
- **直前と同じ素材は選ばない。** 球は3個あって続けて落ちることがあり、
  同じ音が並ぶとそこだけ機械的に聞こえます

file:// で直接開いた場合は fetch が通らないので鳴りません（フィーバーの映像の音と同じ制約）。

AudioContext はユーザー操作なしには始動できないので、最初のクリック/キー入力で resume します。
`M` キーで消音。

---

## 7.6 落とし口の勾配と場内枚数【重要】

### 落とし口の登り勾配

手前のテーブル（z: 3.0〜6.0）を X 軸まわりに **-0.06 rad（約3.4°）** 傾け、
落とし口に向かう登り勾配にしてあります。前縁の天面は y ≒ +0.18 まで上がります。

- 後端の天面がちょうど y=0 で `tableMain` と繋がるよう、箱ごと 0.09 持ち上げてあります。
  中心で回すと後端が沈み、そこに段差ができてかえってメダルが落ちます
- コライダーは `RigidBodyDesc.setRotation()`、メッシュは `mesh.rotation.x` で同じだけ傾けます
  （`layout.*.rx` があれば `createFixedBox` と `Cabinet._box` が拾う）
- メダルは静止摩擦で自然には戻りません（tan 3.4° = 0.06 < μ 0.26）が、**球は転がって戻ります**。
  結果としてボールのほうが落ちにくくなりました（抽選の間隔が 23秒 → 約30秒）

### 場内枚数と処理速度

勾配で山を厚くできるようになったので、上限を **585枚**（従来の3倍）にしました。
ただし枚数と1ステップの計算時間はほぼ比例します（実測）。

| 場内 | 60秒で場外へ | 1ステップ | 60fps 予算(16.7ms)に対して |
|---|---|---|---|
| 200 | 128 | **5.1 ms** | 31% |
| 300 | 763 | **10.2 ms** | 61% |
| 400 | 891 | **16.2 ms** | 97%（限界）|
| 500 | 1841 | **24.4 ms** | 146%（60fps を維持できない）|

**上限まで詰めると動きません。** ただし実際にはそこまで溜まりません。
払い出しが枚数に対して急に増えるため、場内枚数は自然に釣り合います（実測・240秒）。

| 投入間隔 | 落ち着く枚数 | 1ステップ | 払い戻し |
|---|---|---|---|
| 0.55秒に1枚（ふつう）| 160〜200 | 5.5 ms | 95% |
| 0.16秒に1枚（連打）| 200〜230 | 7.3 ms | 83% |

※ この表は**下段を奥へ広げる前**の値です。広げたあとは同じ条件で落ち着く枚数が
1.3 倍ほどになります（§7.10 に測り直した値）。

585 は「一気に払い出したときに溢れないための余裕」であって常用する枚数ではありません。
そのため **ホッパーの `fieldLimit` は 330** に抑え、大当たりでも場内が重くなりすぎないようにしています。

なお勾配を付けた結果、払い戻しは **下がるどころか上がりました**（従来 75% → 83〜95%）。
山が厚くなったぶん力の連鎖が繋がりやすく、押し出しそのものはむしろ効くようになります。
落ちにくくなったのは1枚あたりの体感の話で、詰まりにも強くなりました（AntiJam の発動が 0 回）。

### ソルバ反復を下げても速くならない

`solverIterations` を 8 → 4 にすると接触1件あたりは軽くなりますが、
押し出しが弱くなって場内枚数が 350 前後まで増えるため、**1ステップはむしろ遅くなりました**
（7.3ms → 8.6ms）。速くしたいなら枚数を減らすほうが効きます。

---

## 7.7 上段の壁

上段（プッシャー上面）の**左右**に低い壁を立てています（x = ±6.0、高さ 0.30）。
プッシャー本体と同じ剛体に付けたコライダーなので、往復にそのまま追従します。

### 手前を塞いではいけない

最初は手前の縁に壁を立てましたが、**これは機械を止めます。**
上段は下段への供給源（§2.4）なので、手前を堰き止めるとメダルが下段に降りてこなくなります。

| 手前の壁 | 払い戻し | 場内 | 1ステップ | AntiJam |
|---|---|---|---|---|
| なし | 85% | 194 | 5.2 ms | 0 |
| 高さ 0.10（全幅）| **36%** | **428** | **19.0 ms** | 17 |
| 高さ 0.18（中央に6.0の開口）| 96% | 194 | 7.7 ms | 0 |

中央に開口を残せば両立はできましたが、**手前の壁は現在は付けていません**（`pusherLip` は削除）。

### 側壁の効果は測定できる範囲では出ていない

側壁は入っていますが、正直に書くと**メダルの落ち方に有意な差は出ませんでした。**
150秒・0.55秒に1枚投入で各2回ずつ:

| 側壁 | 払い戻し | ロスト率 | 場内（上段/下段）| 1ステップ |
|---|---|---|---|---|
| なし | 88% / 84% | 26% / 31% | 184 / 172 | 6.2 / 7.0 ms |
| あり (±6.0, h=0.30) | 81% / 93% | 31% / 28% | 187 / 156 | 7.2 / 7.0 ms |

差はどれも試行ごとのばらつきの範囲です。理由ははっきりしていて、
**上段はもともと筐体の側壁（x = ±7.0、高さ12）で完全に囲われている**からです。
プッシャーは幅 14.4 で左右の壁に食い込ませてあるので、上段から横に落ちる経路は元々ありません。

つまり今回の側壁は、上段が「縁のあるトレイ」として見えるようになる、という視覚的な意味が主です。

### ロストを本当に減らしたいなら

メダルが失われているのは上段ではなく**下段の手前左右にあるサイドポケット**
（x = 5.5〜7.0、z = 3.0〜6.0）です。ここを減らすなら、

- `layout.pocketX` を広げてポケットの口を狭める
- サイドポケットの手前にも固定の柵を立てる（＝ポケットを実質なくす）
- 下段テーブルの左右にも勾配を付けて中央へ寄せる

のいずれかになります。上段側をいくら囲ってもここには効きません。

---

## 7.8 持ち枚数とゲームオーバー

プレイヤーは **300枚**（`CFG.wallet.start`）から始めます。投入で1枚減り、
払い出し口に落ちるたび1枚増えます。サイドポケットに落ちたぶんは投入時にすでに引かれているので、
記録だけ残して何もしません。

### 0枚になった瞬間には終わらせない

「最後の1枚を入れたら山が崩れて10枚返ってきた」は普通に起きるので、
0枚になった瞬間に終了すると理不尽です。`Wallet` は
**0枚のまま `gameOverDelay`（8秒）1枚も増えなかったとき**に終了とします。
途中で1枚でも入れば計測はリセットされます。

終了表示中は `earn()` を無視します。これが無いと、終了画面を出したあとに
場のメダルが落ちて持ち枚数が増え、「0枚なのにゲームオーバー」という矛盾した表示になります。

### リスタートでフィールドは初期化しない

フィールドのメダルは筐体の在庫であってプレイヤーの持ち物ではないので、そのまま残します。
実機で言えば「メダルを買い足して続きから遊ぶ」動作です。
物理をリセットしないぶん、リスタート時にひっかかりも起きません。

`best`（最高持ち枚数）は通算の記録なのでリスタートしても引き継ぎます
（`reset(true)` で完全初期化）。

---

## 7.9 スロットの表示を筐体の中に置く

### なぜ移したか

最初、抽選の演出は React のオーバーレイとして画面中央に出していました。
見た目は作りやすいのですが、**演出のたびに盤面の一番おいしいところが隠れます**。
メダルが押し出されて落ちる瞬間はこのゲームの見せ場なので、そこに蓋をするのは本末転倒でした。

そこで **筐体前面（ガラスのすぐ内側）に貼った液晶パネル**として、3D シーンの中に描いています。
実機のスロット付きプッシャーと同じ配置です。

### 実装

`SlotDisplay` が Canvas 2D に描いた絵を `THREE.CanvasTexture` として板に貼っています。

- 材質は `MeshBasicMaterial`（`toneMapped: false`）。ライティングの影響を受けず、液晶らしく自己発光する
- **描き直すのは変化があったときだけ。** リールが回っている間は 18Hz、
  止まっていれば更新しない（`_dirty` フラグ）。毎フレーム 1024×501 を再アップロードすると無駄が大きい
- 絵柄は当初 Canvas の `fillText` で絵文字をそのまま描いていたが、
  **3x3 化に合わせて SVG のパスへ置き換えた**（`render/SymbolArt.js` / DESIGN_GIMMICKS.md §3.10）。
  絵文字は OS ごとに字形も色も違い、HUD 側の DOM と見た目を揃えられないため
- 進行役も `SlotDisplay` が兼ねる。`SlotMachine` からは `await play(res)` するだけで、
  React だろうが 3D だろうが関係ない（この構造は DOM 版から変わっていない）

### 置き場所の制約

パネルの位置は上下から挟まれます。

- **上**: 画面上部の PAY TABLE パネル（HUD）に掛からないこと
- **下**: 上段デッキに掛からないこと。盤面を隠したら移した意味がない

カメラ（`(0, 11, 22)` から `(0, 1, 0.5)` を注視、fov 38°）に対して、
パネルは z=6.1 とかなり手前にあるので見かけが大きくなります。
world 座標では y=6.15・高さ2.6 と控えめな値で、画面上ではちょうど HUD とデッキの間に収まります。
`CFG.slot.display` を触るときはこの2つの制約を確認してください。

ガラス（`transmission: 0.9`）越しに見えるので色はやや眠くなりますが、
「ガラスの内側にある表示器」としてはむしろ自然です。

---

## 7.10 下段フィールドの奥行き

下段（プッシャーの前に広がる平場）を **奥へ 1.0 unit 深くしました**。
露出している下段の奥行きは平均 4.8 → 5.8 unit（**+21%**）になります。

### 広げる方向は「奥」しかない

手前（+Z）側は詰まっています。前縁 z=6.0 のすぐ先に払い出しスロープ（z: 6.05〜8.24）、
発射口（z=6.15）、液晶（z=6.1）、ガラス（z: 6.25〜6.55）が並んでいて、
1 unit 前に出すには前面の役物を全部動かすことになります。
奥（-Z）側は背面壁の裏に何もありません。

### 上段の奥行きは 1 mm も変えていない

上段デッキの奥行きは「背面壁の内面からプッシャー前面まで」で決まります。
**背面壁とプッシャーを同じ量だけ奥へ下げれば、この差は変わりません。**

| | 変更前 | 変更後 |
|---|---|---|
| 背面壁の内面 | z = -2.0 | z = -3.0 |
| プッシャー前面（前進端）| z = 2.15 | z = 1.15 |
| **上段デッキの奥行き** | **4.15** | **4.15**（不変）|
| 下段（前面 〜 前縁 6.0）| 3.85 .. 5.75 | 4.85 .. 6.75 |

深いデッキがメダルを溜め込んで下段に降ろさなくなるのは §2.4 と実測済みなので、
ここは動かしてはいけない値です。

### 一緒に -1.0 したもの

上段デッキ上の「相対位置」で意味が決まっているものは、まとめて奥へ送ります。
据え置くと、デッキの上での位置が変わって挙動が動きます。

| 設定 | 変更前 → 変更後 | 据え置くとどうなるか |
|---|---|---|
| `launcher.landZ` | -1.6 → -2.6 | 着地が背面壁から 1.4 離れ、壁で堰き止める連鎖（§2.4）が弱る |
| `hopper.z` | 1.4 → 0.4 | プッシャー前面より手前に落ちて「下段に直接ぶちまける」になる |
| `ball.spawnZ` | 0.6 → -0.4 | 戻した球がデッキに乗らず、すぐ下段へ落ちる |
| `layout.spawn.z` | -0.6 → -1.6 | デバッグの一括投入だけ |
| `jackpot.tower.z` | -1.2 → -2.2 | 後退したプッシャーの上面から外れ、崩しにいく前進で運ばれない |
| 初期配置の格子 | 上段 -1.8 → -2.8 / 下段は 3列 → 4列（z: 1.6 起点）| 奥が空いたまま始まる |

`launcher.yawRange` だけは -1.0 ではなく別の理由で動かしています。
着地の左右幅は **射程 × sin(yawRange)** で、landZ を送ったぶん射程が 7.75 → 8.75 に伸びます。
0.70 のままだと端の着地が x=±5.0 → ±5.6 まで外へ出て、上段の側壁（内面 ±5.875）に
当たって跳ね返るようになります。`asin(5.0 / 8.75) = 0.608` に絞って、
着地の扇は以前とほぼ同じ幅（±5.0）に保ってあります。

### 場のメダルも増やす

下段の面積が +24%（14.0 幅の帯が 1.0 伸びる）なので、下段に敷く枚数を同じ密度で増やしました。
上段は形が変わっていないので据え置きです。

```
defaultFieldStock  231 → 246
deckRatio         0.73 → 0.68     （上段 : 下段 = 168 : 63 → 168 : 78）
```

### 【実測】減るのは払い出しではなく、横に逃げる

0.16秒に1枚を撃ち続け（360枚/分）、120秒の窓を2回。
2分の窓では 500枚のクルーンと JP が当たり外れで支配的になるので、この2つは止めて測っています。

| | 変更前 | 変更後 |
|---|---|---|
| 払い戻し | 78.6% / 74.3% | **68.5% / 71.2%** |
| ロスト（サイドポケット）| 12.8% / 14.9% | **25.6% / 18.7%** |
| チャッカー | 15.0% / 13.1% | 14.4% / 12.8% |
| 落ち着く場内枚数 | 197 / 199 | **269 / 261** |
| 1ステップ | 5.52 ms（188枚）| **8.05 ms（277枚）**|

**場外へ出ていく総量は変わっていません**（払い戻し + ロスト + チャッカー ≒ 104%）。
変わったのは配分で、払い戻しが 6〜7 ポイント減ったぶんがそのままロストに移っています。

理由は下段を押される距離が伸びたことです。下段の山は前縁まで 1.0 ぶん長く
押されるようになり、圧が掛かった山はそのぶん横へも広がります。
下段テーブルは z=3.0 まで幅 14.0（±7.0）で、そこから先が幅 11.0（±5.5）に絞られて
サイドポケットになるので、**z=3.0 に着くまでに ±5.5 の外へ広がっていた枚数がそのまま
ロストになります**。上段の側壁（§7.7）は関係ありません。あれは元々
筐体の壁で囲われている上段の話で、実測でも有意差は出ていません。

減らしたいなら §7.7 の末尾に挙げてある3つ（`layout.pocketX` を広げる /
ポケットの手前に柵を立てる / 下段テーブルの左右にも勾配を付ける）が、
そのままここにも効きます。

ロストは JP メーターの燃料（DESIGN_GIMMICKS.md §3.4）なので、JP の貯まり方は 1.5 倍ほど速くなります。
渋くなったぶんを戻したいなら、効きの大きい順に:

1. `pusher.strokeHalf` … §5 の実測表のとおり、払い出し量にいちばん効く
2. `slot.symbols[].pay` … ホッパーからの供給量が増える
3. `layout.tableFrontTilt` … 落とし口の登り勾配を浅くする（§7.6）

### 1ステップが 5.5 → 8.0 ms に増えた

場内が自然に釣り合う枚数が 198 → 265 に増えたためで、§7.6 の枚数対コスト表どおりです。
60fps の予算 16.7ms に対して 33% → 48%。PC では問題ありませんが、
**モバイルでは物理が余裕を食う側に回ります**（描画側は §13.2 で削ってある）。
重い端末で足りなくなったときに最初に触るのは `hopper.fieldLimit`（330）です。

---

## 8. 実装フェーズ（完了状況）

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | Vite + three + Rapier 起動 | 済 |
| 1〜2 | メダルの円柱挙動 / 山の安定 | 済（めり込み 0 を確認） |
| 3 | プッシャー往復・押し出し | 済（§2.4 の修正を経て成立） |
| 4 | InstancedMesh + プール + 300枚負荷 | 済（1ステップ 3.4ms = 60fps 予算の 20%） |
| 5 | 投入・落下・クレジット | 済 |
| 6 | 筐体の作り込み | 済（壁・ガラス・フレーム・照明・サイドポケット） |
| 7 | 抽選ボール・スロット・効果音 | 済（§7.4 / §7.5） |
| 8 | 持ち枚数・ゲームオーバー | 済（§7.8） |
| 9 | セーブデータ | 済（§11） |

---

## 9. リスクと対策（実測結果つき）

| リスク | 症状 | 対策 | 結果 |
|---|---|---|---|
| 薄い円盤の貫通 | 床/壁をすり抜ける | 単位スケーリング、CCD + soft-CCD、厚さ 0.10 | 脱走 0 |
| 着弾時のめり込み | 一時的に天板に沈む | 投入高さを 11 → 7 に下げる | 沈み 0 |
| **山が前に進まない** | **払い出しが起きない** | **§2.4（壁潜り込み + 浅いデッキ）** | **払い出し成立** |
| 満杯で詰まる | 投入できなくなる | 山からは抜かず投入を断る。落ちれば空く | 平衡枚数 150〜190 |
| クリックの取りこぼし | 押しても出ない時がある | 押下をラッチして次ステップで消費 | 解消 |
| ボールが山に埋もれる | 抽選がいつまでも回らない | 摩擦を 0.10 に下げ、戻す位置を調整（§7.4） | 平均20〜30秒に1回 |
| ボールが挟まって固まる | 同上 | 10秒動かなければ自分にインパルスを与える | 300秒で20回前後 |
| **戻った球が即落ちる** | **スロットが1〜3秒間隔で連続する** | **戻り直後2.2秒は settling 状態にして静止させる（§7.4）** | 2秒以内の落下 0〜3回/300秒 |
| 接触音が鳴りっぱなし | 山の微小接触まで拾う | `impactThreshold` を 3200 に | 0.5件/ステップ |
| **演出パネルが半透明で固まる** | スロットやゲームオーバー画面が薄く張り付く | **`transition` も keyframes も opacity を動かさない。出現は transform だけで付ける** | 解消 |
| **場内が詰まって払い出しが止まる** | 押しても1枚も落ちない | 落とし口の勾配で山を厚くできるようにした + AntiJam（§7.6） | AntiJam 発動 0 回 |
| 枚数を増やしすぎる | 1ステップが 20ms を超えて 60fps が出ない | 上限は 585 だがホッパーは 330 で止める（§7.6） | 通常プレイで 5.5ms |
| **上段の手前を壁で塞ぐ** | **下段にメダルが降りてこず詰まる** | **手前には壁を付けない。側壁のみ（§7.7）** | 払い戻し 36% → 88% |
| フレーム落ち | 枚数が増えると重い | 上限300、スリープ、InstancedMesh | 3.4ms/step |

---

## 10. 動かし方

```bash
npm install
npm run dev        # http://localhost:5173
npm run build      # dist/ に出力（相対パスなので静的配信でそのまま動く）
npm run typecheck  # UI の型チェック（Vite のビルドは型を見ないので別途）
```

操作: クリック / Space で発射（狙いは自動で首振り・長押しで連続）、
右ドラッグで視点、ホイールでズーム、**M キーで消音**、
**D キーでデバッグパネル**（パラメータ調整・抽選の強制実行・コライダー表示）。

> **注意**: `node_modules` は同梱していません。Vite/Rolldown はプラットフォーム固有の
> ネイティブバイナリを使うため、`npm install` は実行する環境で行ってください。
> `_to_delete/` はこちらで作った古い成果物なので、丸ごと削除して構いません。

---

## 11. セーブデータ

> 実装済み。`src/save/SaveStore.js` と `Wallet` の2階建て化が本体です。

### 11.1 盤面の座標は保存しない

草案では場のメダル数百枚の位置・姿勢・速度を保存する前提で設計していましたが、
**やめました。** 保存するのは「場に何枚あったか」という数字1つだけにして、
並びは毎回作り直します。

やめて消えたもの:

| 草案で必要だったもの | |
|---|---|
| 37 KB のスナップショット | → 数百バイトの JSON に |
| レイアウトの指紋と不一致判定 | → **不要**。座標を持たないので壁を動かしても壊れない |
| 復元時に剛体を弾けさせない4手順 | → **不要** |
| キーを2つに分ける設計 | → 1つで足りる。書き込みは 1 回、原子性の心配も消える |

失うのは「前回と寸分違わぬ山」だけです。**体験として効いているのは山の厚みであって、
1枚1枚の並びではありません。** 枚数さえ合っていれば「厚い台の続き」として成立します。

この開発中に筐体のレイアウトを5回変えたことを踏まえると、
**座標を持たない設計は「今後レイアウトを触っても壊れない」という保険**でもあります。

### 11.2 保存するもの（キー1つ、小さな JSON）

```jsonc
// localStorage キー: "medalweb:v1:save"   —— これ1つだけ
{
  "v": 1,
  "userId": "3f2b9c1e-7a44-4d10-9f3e-8c2d5b1a6e07",  // §11.3
  "savedAt": 1788486000000,

  "medals": 284,          // 持ち枚数
  "fieldStock": 218,      // 場に何枚あったか。並びは保存しない
  "best": 486,

  "run":      { "inserted": 742, "earned": 442, "lost": 118 },
  "lifetime": { "inserted": 5210, "earned": 3180, "lost": 900, "games": 7,
                "slot": { "spins": 142, "wins": 81, "byId": { "replay": 47, "bell": 12 } } },

  "settings": { "muted": false, "volume": 0.55 }
}
```

全部で数百バイトです。**サイズを気にする必要がないので、毎回まるごと書きます。**

#### run と lifetime を分ける

いまの `Wallet` の `inserted / earned / lost` はリスタートで 0 に戻ります。
保存する以上、消えては困る数字が別に要るので2階建てにします。
`Wallet.reset()` が触るのは `run` と `medals` だけ、`lifetime` は加算しかしません。
`best` は lifetime 側（すでにリスタートで引き継ぐ実装になっています）。

#### fieldStock の使い方

`prefill()` を「枚数を受け取って、その枚数ぶんの山を作る」形に変えます。
格子の層数を枚数から逆算するだけで、いまの実装からの差分はごくわずかです。

```js
prefill(pool, count)   // count 枚になるまで上段→下段の順に積む
```

読み込み時は `Math.min(fieldStock, CFG.medal.maxCount)` でクランプします
（あとから上限を下げても壊れないように）。

### 11.3 ユーザー識別子

初回起動時に `crypto.randomUUID()` で1つ生成し、以後変えません。
**いまは使いません。** 将来サーバ連携（ランキング、端末間の引き継ぎ）を足すときに
「このブラウザのプレイヤー」を名乗るためのもので、スキーマに最初から入れておくのが安いので入れます。

**注意しておくべきこと:**

- **これはブラウザを識別するもので、人を識別するものではありません。**
  同じ人が別のブラウザを開けば別人になり、履歴を消せば消えます
- したがってサーバを足すとき、**この ID を認証の代わりにしてはいけません。**
  「引き継ぎコード」程度の扱いにとどめ、本人確認が要るなら別途用意します
- ランダムな UUID なので個人情報ではありませんが、識別子ではあります。
  §11.6 の「記録を消す」では **ID も一緒に消え、次回は新しい ID が振られます**

### 11.4 いつ書くか

```
値が変わったら 2秒デバウンス + pagehide / visibilitychange(hidden)
```

- **`beforeunload` は使いません。** モバイルのアプリ切り替えやタブ切り替えで発火しないためです
- `pagehide` の中では非同期処理ができません。localStorage の同期APIはここでは長所です
- 書き込みは必ず try/catch。`QuotaExceededError` とプライベートモードの拒否を握りつぶし、
  「保存できていない」ことだけ HUD に小さく出します（黙って消えるのが一番不親切）

### 11.5 読み込み

```
起動
 ├ load()
 │   ├ 無い / 壊れている / v 不一致 → 新規（userId を生成、300枚、初期配置は既定の枚数）
 │   └ ある
 │       ├ userId / lifetime / best / settings を復元
 │       ├ 保存時が gameOver だった → 持ち枚数は 300 に戻し、run を新規にする
 │       └ fieldStock 枚で prefill する
 └ 例外が出たらセーブを捨てて新規に落ちる（起動しないほうが困る）
```

**ゲームオーバーの状態は復元しません。** 開いた瞬間に終了画面が出るのは体験として悪く、
「もう一度」を押すだけの無意味な操作を強いるだけです。
`lifetime` と `best` と `userId` は引き継ぎ、持ち枚数と `run` だけ新規にします。

### 11.6 保存しないもの

- **場のメダルの座標**（§11.1）
- **デバッグパネル（D キー）で触った物理パラメータ。** これは明確に保存しません。
  この開発では摩擦・ストローク・しきい値を何十回も詰めましたが、
  壊れた値が保存されて残ると「なぜか払い出しが止まる台」ができあがり、
  原因の切り分けが極端に難しくなります。物理は毎回 `CFG` の既定から始めます
- スロット演出の途中状態。中断扱いにし、保留数は持ち越しません
- カメラの位置。変な角度で固定されると戻し方が分からなくなるので既定に戻します

### 11.7 API

```
src/save/SaveStore.js   これ1ファイル。localStorage を知っているのはここだけ
```

```js
SaveStore.available          // localStorage が使えるか (プライベートモード対策)
SaveStore.load()             // → save | null   壊れていたら null
SaveStore.save(state)        // 2秒デバウンスは SaveStore が持つ
SaveStore.flush()            // pagehide から即時に書く
SaveStore.clear()            // userId ごと消す
```

`Wallet` は `SaveStore` を知りません。`main.js` が `wallet.onChange` の中で
`SaveStore.save()` を呼ぶ形にすれば、ゲーム側は保存の存在を意識せずに済みます。
UI を `mountUI` の裏に隠しているのと同じ構造です（§6.5）。

### 11.8 消す導線

**設定パネルは作らず、ゲームオーバー画面に「記録を消す」を置きます。**
新しい画面を1つ作るより、すでにある画面に足すほうが安く、
かつ記録をリセットしたくなるのはたいていゲームオーバーの直後です。
誤爆しないよう確認を1段挟み、**ユーザーIDも消えて次回は新しいIDになる**ことを明記します。

### 11.9 実装規模

盤面を捨てたことで、段階に分ける必要がなくなりました。

| 作業 | 内容 |
|---|---|
| `src/save/SaveStore.js` | 新規1ファイル。読み書き・デバウンス・try/catch |
| `Wallet` | `run` / `lifetime` の2階建てに分ける |
| `prefill()` | 枚数を引数で受け取れるようにする |
| `main.js` | `wallet.onChange` から save、起動時に load、`pagehide` で flush |
| ゲームオーバー画面 | 「記録を消す」ボタン |

**半日かからない規模**です。将来サーバを足すときも、`SaveStore` の中身を差し替えるだけで、
ゲーム側には触りません。

---

### 11.10 実装後のメモ

実測: セーブデータは **319 バイト**（持ち枚数・通算成績・スロットの絵柄別回数・設定を含む）。
localStorage の上限に対して誤差のような大きさで、頻度も気にする必要がありませんでした。

検証したこと（ヘッドレスブラウザで実際にリロードを挟んで確認）:

| | 結果 |
|---|---|
| 持ち枚数・通算成績・最高記録の復元 | 一致 |
| `userId` の維持 | 一致 |
| 消音設定の復元 | 復元される |
| 場の枚数の復元 | 保存 227枚 → 復元後 223枚（落下中のぶんの差） |
| 「記録を消す」 | 通算・最高記録が 0 に戻り、**userId も振り直される** |

#### 踏んだ落とし穴: userId を const で持っていた

`main.js` で `userId` を起動時に一度だけ読んで `const` に入れていたため、
「記録を消す」を押しても**同じ ID が書き直されて**いました。
消したのに識別子が残るのでは消したことになりません。`let` にして、
クリア時に `SaveStore.newUserId()` で振り直すようにしています。

セーブ機能は「書けているか」より「消えているか」のほうがテストを忘れやすい、という教訓です。

---

## 12. 拡張余地

### 12.1 実装済み → [DESIGN_GIMMICKS.md](DESIGN_GIMMICKS.md)

チャッカー・保留オッズ・フィーバー・プログレッシブJP・メダルタワー・
フリッパー・3段クルーンを追加しました（特殊メダルと台パンは追加後に削除。
DESIGN_GIMMICKS.md §3.7 / §3.8 に記録だけ残してあります）。詳細は別書に譲りますが、
**本書の記述に影響する変更が3つ**あるので、ここに要点だけ残します。

| 変更 | 影響する節 |
|---|---|
| **プッシャーの位相を絶対時刻から積分に変えた** | §7.1。`period` を実行中に変えると位相が飛ぶため。`syncMesh()` は `prevZ / z` の補間になった |
| **手前テーブル (`tableFront`) を Fixed → キネマティックにした** | §4 / §7.6。フィーバー中に勾配を反転させる。傾きを変えるときは y も一緒に動かすこと |
| **前縁 (z=6.0) の先に払い出しスロープを足した** | §7.3。落下判定が3分類（払い出し / ロスト / チャッカー）になった |
| **スロットの演出を多層化し、配当を約1.8倍にした** | §7.4 / §7.9。演出は1回 約3.6秒（旧 2.5秒）。配当と払い戻しの関係は DESIGN_GIMMICKS.md §4.2 |

そして、本書 §2.4 と §7.7 の教訓がそのまま再発しました。

> **盤面の手前側に物を置くと、向きに関係なく機械が止まる。**
> 前縁に開けたチャッカーの穴（力の連鎖が切れる）と、
> 盤面に常設したフリッパー（払い戻し 75% → 15%）の2件で踏んでいます。

### 12.2 まだ手を付けていないもの

- **サイドポケットの演出**: ロスト時の減算表示（音は付いた）
- **リーチ演出の強化**: 図柄ごとの信頼度、カットイン
- **BGM**: WebAudio で簡単なループを合成する（ファイルを増やさずに済む）
- **視覚品質**: SSAO、被写界深度、メダルの刻印テクスチャ

---

## 13. モバイル対応（PC / モバイルの出しわけ）

対象は iPhone の Safari。前提は**「PC とモバイルでゲーム自体が変わらないこと」**です。

### 13.1 何を出しわけて、何を出しわけないか

出しわけの境界は `src/core/Device.js` 一箇所にあります。ここが `IS_MOBILE` と
描画品質 `QUALITY` を決め、`<html data-device="mobile">` を立てます。
CSS も UI も 3D 側もこの1つを見るので、判定が2箇所に散りません。

| | 出しわける | 理由 |
|---|---|---|
| 影 / アンチエイリアス / 解像度 / ガラスの屈折 / メダルの分割数 | ○ | 見た目だけ。触っても盤面の結果は動かない |
| 画角・注視点 | ○ | 縦持ちでは合わせないと**筐体が画面に入らない** |
| HUD の配置と量 | ○ | 390px 幅に PC の HUD は載らない |
| `CFG.physics.*` / `CFG.medal` の物理量 / `CFG.launcher` / 各ギミックの当選率 | **×** | 触った瞬間に「同じ操作で違う結果が出る台」になる |

出しわけたあとで、**両者の設定が本当に一致しているかを実測**しました。
`CFG` の物理まわりと、そこから逆算される初速・射程・コライダー数を
まとめて突き合わせています。

| 項目 | PC | モバイル |
|---|---|---|
| 発射初速 `launcher.speed` | 56.392816156273966 | **同じ** |
| 射程 `launcher.range` | 7.749999999993196 | **同じ** |
| 剛体 / コライダー数 | 601 / 612 | **同じ** |
| `CFG.physics` `CFG.medal`(分割数以外) `CFG.layout` `CFG.launcher` `CFG.kuruun` `CFG.fever` `CFG.jackpot` `CFG.special` `CFG.slot` | — | **差分ゼロ** |
| メダルの円柱の分割数 | 24 | **12**（唯一の差分） |

分割数だけは違いますが、コライダーは Rapier の cylinder を別に作っているので、
積み方にも落ち方にも影響しません。

### 13.2 描画で削ったもの

| 削ったもの | なぜ効くか |
|---|---|
| 影 (`shadowMap`) | 2048 の影マップへ全キャスターをもう一度描く。単純に1パス増える |
| **ガラスの屈折 (`transmission`)** | 裏側の景色を**シーンごともう一度描く**。1枚のガラスのために描画が倍近くになる |
| アンチエイリアス | MSAA は帯域を食う。同じ予算なら解像度に回したほうが効く |
| `devicePixelRatio` の上限 1.5 | iPhone は DPR 3。等倍で描くと画素数が9倍 |
| メダルの円柱 24 → 12 分割 | 585枚ぶんの三角形が半分に |
| パネルの `backdrop-filter` | canvas の上に重ねると iOS で目に見えて重い。パネルの枚数だけ全画面合成が走る |
| 液晶のテクスチャ 1024x501 → 640x313 | 電話の画面には過剰。**書き直すたびに全面がアップロードされる**ので、面積がそのまま帯域になる（783KB / 回。等倍だと 2004KB）|

**物理は削っていません。** `timestep` も `solverIterations` も `maxCount` もそのままです。
端末が追いつかない場合は `Loop` の `maxSubSteps` で頭打ちになり、
**結果が変わるのではなく時間の進みが遅くなる**（＝スローになる）だけで済みます。

さらに、それでも足りない端末のために解像度だけ実測で落とします。
2秒ごとに fps を測り、**45 を切ったら 0.25 下げ、55 を超えた計測が2回続いたら 0.25 上げる**
（0.75 〜 端末の上限）。上げ下げの閾値を離し、上げるほうにだけ「2回続けて」を課しているのは、
境目の端末で解像度がぱたぱた往復するのを防ぐためです。

> #### 【実測】「下げるだけ」にしたら、一度重い演出が来ただけで画面が戻らなくなった
>
> 最初は**下げるだけで上げ直さない**実装にしていました。往復するより良いだろう、という判断です。
> これは iPhone Safari で表面化しました:
>
> **フィーバーの映像が流れた瞬間に fps が落ち、25秒のあいだに 1.5 → 0.75 まで
> 3段階降格して、明けても戻らない。** 液晶の文字が読めないレベルまでぼやけました。
> 3D の中に文字があるので、HUD（DOM）と違って解像度が直撃します。
>
> 直しは2つ。
>
> 1. **上げ直せるようにした**（上のヒステリシス）。降格が一方通行だったのが本体の誤り
> 2. **重いと分かっている演出の間は測らない**（`Stage.setQualityHold()`）。
>    25秒で終わるもののために解像度を捨てるのは、得より損が大きい。
>    フィーバーの `onEnter` / `onExit` で掛け外しする
>
> 「一時的な負荷」と「その端末の実力」を区別できない計測は、
> 区別できるところ（=負荷を掛けている側）から教えてもらうしかありません。

> #### 【実測】裏に回っている間の fps を信じてはいけない
>
> タブが非表示のあいだ `requestAnimationFrame` は止まります。戻ってきた最初の計測は
> 「4秒で3フレーム」のような値になり、そのまま信じると**タブを切り替えただけで
> 解像度が落ちます**。計測窓が 4秒を超えたぶんは捨てています。

### 13.3 縦持ちで筐体が入らない

縦持ちの iPhone は aspect が 0.46 ほどになります。fov 38 のままだと横に見えるのは
7 unit しかなく、幅 15.4 unit の筐体は完全にはみ出します。

`CFG.render.fitWidth / fitHeight` で「最低でもこの箱は映す」と決めて、
収まるところまで縦の画角を広げます（16:9 より横長では条件を最初から満たすので、
**PC の見え方は 1px も変わりません**）。

広げるだけだと縦持ちで 105度になって歪むので、`maxFov`(62度) で頭を打ったら
残りはカメラを引いて稼ぎます。`d` を f 倍して `tan(fov/2)` を 1/f 倍すれば
`d·tan` は変わらないので、映る範囲は同じまま歪みだけが減ります。

| 画面 | aspect | fov | カメラ距離 |
|---|---|---|---|
| PC 16:9 | 1.78 | 38（既定のまま） | 23.7 |
| iPhone 横持ち 812×375 | 2.17 | 38（既定のまま） | 23.5 |
| iPhone 縦持ち 375×812 | 0.46 | 62（頭打ち） | 29.5（1.29倍に引く） |

縦持ちのときだけ注視点を y=1.0 → 3.4 に上げています。筐体が画面の中ほどに来て、
空いた上下がそのまま HUD の置き場になります（実測: 筐体は 812px 中 202〜628px を占める）。

向きが変わったときだけ書き戻すこと。毎回書き戻すと、プレイヤーが動かした視点を奪います。

> #### 【実測】表示領域はイベントに頼らず毎フレーム測る
>
> 最初は `resize` イベントだけで測っていて、2つ踏みました。
>
> - 起動直後の一瞬、要素の実寸が 0 で返ることがある。これを掴んだまま
>   `resize` が来ないと、**以後ずっと 1x1 のまま**になる（aspect 1 で描かれる）
> - iOS Safari はアドレスバーの出入りで表示領域が変わるが、`resize` が飛ばないことがある
>
> `#app` の `clientWidth` を毎フレーム読み、変化が無ければ即座に返る形にしました
> （three.js の作法でもある）。実寸が 2px 未満なら**掴まずに次のフレームに任せます**。
>
> `window.innerHeight` は使いません。iOS ではアドレスバーの出入りで値が動き、
> canvas が表示領域より縦に長くなって下端がはみ出します。

### 13.4 タッチ操作

「タイミングを狙って撃つ」台なので、**指1本は発射に使い切ります**。
指1本で視点が回ると、撃つたびに画面が動いてしまう。

| 操作 | 割り当て |
|---|---|
| 1本指タップ / 長押し | 発射（PC のクリック / Space と同じ経路） |
| 2本指 | 視点回転・ズーム（`OrbitControls.touches.TWO`） |

2本目の指が乗ったら握りを解いて視点操作に譲ります。そうしないと、
視点を回そうとするたびに1枚出ていきます。

> #### 【実測】iOS で気をつけたこと
>
> | 症状 | 対処 |
> |---|---|
> | 撃とうとするたびに画面がスクロール・ダブルタップで拡大 | `#app` に `touch-action: none` |
> | ピンチで拡大できてしまう（Safari は `user-scalable=no` を無視する） | `gesturestart/change/end` を `preventDefault` |
> | 長押しで選択メニューが出る | `-webkit-touch-callout: none` |
> | 通知や画面端のスワイプで `pointerup` が来ず、**メダルが出続ける** | `pointercancel` も拾って握りを解く |
> | ノッチ・ホームインジケータに HUD が潜る | `viewport-fit=cover` + 各パネルを `env(safe-area-inset-*)` で逃がす |
> | 音が鳴らない | `AudioContext` の生成と `resume()` を最初の `pointerdown` の中で同期的に行う（既存のまま動いた） |
>
> タブを戻したときに GPU コンテキストが飛ぶことがあるので、`webglcontextlost` を
> `preventDefault` して復帰するまで描画を止めています
> （止めないと毎フレーム WebGL エラーを吐き続けます）。

### 13.5 HUD をどう削ったか

キーボードが無い端末では、**キーにしか割り当てていない操作は押せなくなります**。
削る前に、そこを埋めるほうが先です。

| | モバイルでどうしたか |
|---|---|
| 消音 (M) | 表示だけだった `SOUND ON` の札を**ボタンにした**（PC でも押せる） |
| 操作説明 | 「タップ=発射 / 2本指=視点」の2つだけに |
| STEP (物理1ステップの ms) | 落とす。狭い画面で場所を食うわりに読まれない |
| 配当表の早見図（5ラインの図） | 縦持ちでは落とす |
| 配当表そのもの | **残す**。縦持ちでは右の縦帯をやめて横一列の帯にし、下段へ移す |
| 保留ランプ | **残す**。「いま何回ぶん抽選が溜まっているか」は判断材料 |

縦持ちでは筐体が画面の中ほどに寄るので、上下に空いた帯へ HUD を逃がしています
（上: 持ち枚数と FPS、下: 配当表 → 操作説明）。
横持ちは PC と同じ配置のまま、既存の `max-height` 用の段組みに任せています。

### 13.6 PC からモバイル版を確認する

`?device=mobile` / `?device=desktop` で判定を上書きできます。
`data-device` 属性ごと切り替わるので、CSS も 3D 側も同時に切り替わります。

```
http://localhost:5173/?device=mobile
```

自動判定は `(hover: none) and (pointer: coarse)` を主に見ます。UA 判定は当てになりません
（iPadOS 13 以降の Safari は Macintosh を名乗るので、タッチ点の有無と併せて見ています）。
タッチパネル付きの Windows ノートはマウスも持っているので `hover: hover` になり、
PC 扱いのままです。
